# Product / Technical Gap Baseline

This baseline is code-current for the Pingora migration stack. Exact source heads, base tips, reviews, workflow/security runs, rulesets, and sibling Context Fabric heads are always re-read live; predecessor evidence never transfers across source, documentation, dependency, base, or governance movement. Moving `context-graph-contracts` / `enterprise-architecture-core` PR state is intentionally not mirrored here; the current cross-loop evidence ledger is `ContextualWisdomLab/.github#1608`.

## Current pg-erd dependency-root repair

Exact predecessor #12 `a48b03f20715b6e708cb26648d16b954ba57be24` acquired hosted execution. CI `34127601556` completed GREEN `load-contract 101759853863` and GREEN `oci-runtime 101759854190`; `test 101759854021` passed exact checkout, Rust 1.98.0 formatting, and the complete locked compile/test suite, then failed only at strict Clippy because `src/edge_routing.rs` still used the Rust 1.98.0 `clippy::unnecessary_sort_by` form. Supply Chain `34127601770` completed GREEN on the same SHA.

Minimal dependency-root repair `beb52d136b7f17b88db6e98ddeffa2479223e768` changes only route ordering to `sort_by_key(|route| std::cmp::Reverse(route.priority))`. It preserves descending priority, duplicate-priority fail-closed validation, first-match route selection, the characterized 110/100/1 contract, and all product/security authority boundaries.

Fresh exact review then found a separate still-valid listener-authority bypass in the same parent: the existing mapped-address check rejected an exact mapped/native alias but still admitted wildcard combinations such as `0.0.0.0:8080` with `[::ffff:127.0.0.1]:8080`, and the same authority class could also appear as mapped-wildcard versus mapped-concrete IPv6 spellings. RED `738e87303c889ace935bc1c98f3ad2d804ca9598` adds both tuple orders for native wildcard/mapped concrete, mapped wildcard/native concrete, and mapped wildcard/mapped concrete. GREEN `35c018a527235c708f853cf9332fdf2dbb47182c` canonicalizes IPv4-mapped IPv6 addresses to mapped IPv4 authority before wildcard/equality comparison while preserving distinct concrete non-aliased addresses. `API_CONFIG_CONTRACT.md` follow-up `4cac7d3990d065f2e578215d421efb2d2bf0331d` states the same fail-closed invariant.

Exact `8db8b4504c7eb291b277f3b891b3ef226ad0c7c6` then acquired hosted execution and exposed a deterministic Rust 1.98.0 formatting RED before compile/test: CI `34146400870` / test `101819278764` identified only `src/migration_admin.rs:216`. Formatter-only `e7d6642a57d6e468c1e9637b3107e70349bb7ab5` applies exactly the hosted rustfmt output; listener semantics and all acceptance thresholds are unchanged.

A separately hosted descendant #15 exact `50c160e6d76c09559f56a1cda3b9269f720757ed` exposed a shared process-test evidence defect after exact compile/test/clippy/rustdoc succeeded: CI `34143255649` / test `101809679920` failed only at the unchanged 100% owned-production coverage gate. Its exact coverage artifact showed the dedicated `src/bin/cwl-pingora-pg-erd-migration.rs` success-serving path at 24/39 lines and 40/79 regions because the shared `tests/pg_erd_production_path.rs` fixture hard-killed the live child in `Drop`, preventing process-local LLVM profiles for the serving path from flushing. The same binary and fixture blobs exist at the #12 dependency root. Test-only `e13f9e3c6c9530497e5d476b1edfef9f95b3ef50` keeps the emergency Drop cleanup but terminates the successful pg-erd process with SIGTERM and waits for Pingora's established graceful shutdown, mirroring the generic production-path fixture. It changes no production source and does not exclude files, reduce the coverage denominator, or weaken the 100% line/region gate.

A later exact descendant coverage receipt also exposed a root-shared structurally unreachable branch in `migration_admin::build_proxy`: pre-listener validation had already proved positive runtime limits before the method repeated fallible `RuntimeIsolationLimits::try_new`. Minimal production repair `74577a25c22bd857dfa31b56185f804f527b7aed` reuses the existing validated construction path without weakening invalid-budget rejection or changing routing, TLS, forwarding, product authority, dependencies, or coverage thresholds.

The parent branch then advanced through ordinary concurrent commits that were read and adopted rather than treated as a race. Exact `079d6cad35ea42d18c81839a9bdc0e13ec0511a1` adds real-listener saturation/recovery traffic: with `max_in_flight_requests=1`, one routed request is held at origin, a second routed request must fail HTTP 503 before reaching origin, and capacity must recover to HTTP 200 after the first lease is released. Its Supply Chain `34156662854`, load-contract `101849803441`, and OCI runtime `101849803392` were GREEN; test `101849803376` failed only at Rust 1.98.0 formatting and produced exactly two diffs in `tests/pg_erd_production_path.rs`.

Formatter-only `850aff53e033ae1268a520d77c108b24aaba8213` applies exactly those two hosted rustfmt changes. CI `34159473276` is terminal GREEN on that exact implementation head: `load-contract 101858081643`, `oci-runtime 101858081794`, and `test 101858081810` all completed success; the test lane passed exact checkout, Rust 1.98.0 formatting, the full locked compile/test suite, strict Clippy, warning-denied rustdoc, the owned-production coverage workload, and the unchanged 100% line/region enforcement. Supply Chain `34159473308` / `candidate-evidence 101858081874` also completed success through committed dependency audit, exact image build, SPDX SBOM, image scan, exact-source binding, and evidence upload.

This baseline update is documentation movement after that implementation GREEN. The `850aff53...` result is therefore validated predecessor evidence, not current-head GREEN; the docs-only successor must reacquire exact CI/Supply Chain and fresh technical review before promotion. Descendants #14 and later pg-erd children must ordinary/non-force adopt the stable repaired #12 lineage in dependency order rather than duplicating root fixes or force-moving refs.

## Shared runtime

| Area | State | Evidence / gap |
| --- | --- | --- |
| Executable Pingora path | Implemented on ancestor branch | Generic production binary composes `GatewayCommand` -> `GatewayConfig` -> `GatewayProxy` -> `http_proxy_service`; the pg-erd candidate adds a separate bounded composition root rather than widening generic v1. Every changed head must reacquire hosted evidence |
| DDD ownership | Implemented | Edge admission invariants live in `edge_contract`; consumer-derived route characterization in `edge_routing`; response-header characterization in `http_policy`; `migration_plan` composes route/header contracts with explicit stable upstream authority; `migration_delivery` binds admitted identities to explicit prevalidated Pingora peers; `migration_admin` owns only the fixed pg-erd profile plus deployment-variable listener/runtime/transport values; `forwarding_policy` owns accepted-transport -> compatibility-forwarding trust; `runtime_isolation` owns body/concurrency admission; `observability` owns payload-free low-cardinality completion facts/counters/log shape; internal `process_health` owns gateway-local `/livez` and `/readyz`; `migration_proxy` is the Pingora callback adapter. Product auth/business policy, certificate issuance/rotation, Wardnet/EgressWeave decisions, and Keyverse identity remain outside this boundary |
| Fail-closed generic config | Implemented | Strict YAML, version/body/upstream/TLS/trust-path/timeout validation; generic `GatewayConfig` v1 deliberately remains exactly one upstream and is not silently repurposed as a multi-route configuration language |
| Admin Config | Implementation predecessor exact GREEN; docs-only head pending | Draft PR #12 adds strict `PgErdMigrationConfig` and a separate `cwl-pingora-pg-erd-migration` composition root. Operator input may set listener/metrics sockets, non-zero body/in-flight/keepalive budgets and concrete `backend`/`frontend` transport/TLS values, but cannot define routes, response headers, product auth/business logic, service discovery, or new migration authorities. Listener collision validation covers equal sockets, same-family wildcard aliases, IPv6-wildcard/IPv4 dual-stack ambiguity, exact mapped/native aliases, native/mapped wildcard combinations and mapped-wildcard/mapped-concrete aliases. Parsing validates exact authority without loading custom trust bytes; peer/trust materialization occurs once during `build_proxy` before listeners. Exact implementation predecessor `850aff53...` passed compile/test/rustdoc/100% coverage; current docs-only head must reacquire gates |
| Edge Routing | Dedicated compiled-listener predecessor GREEN; docs-only head pending | Draft parent PR #5 captures `pg-erd-cloud` Traefik precedence as executable exact/prefix selection. PR #11 consumes it from Pingora callbacks. PR #12's dedicated loopback process contract requires `/healthz -> backend`, raw `/apiary -> backend`, and fallback product traffic -> `frontend`; `850aff53...` passed exact compile/test/coverage, but no parity/shadow/cutover credit transfers across this documentation movement |
| HTTP Policy | Dedicated compiled-listener predecessor GREEN; docs-only head pending | Draft parent PR #6 captures the four pg-erd response-security fields. PR #11 applies them with replacement semantics. PR #12's loopback process contract requires an origin `X-Frame-Options: SAMEORIGIN` value to become characterized `DENY` together with the other three fields; `850aff53...` passed exact compile/test/coverage and current docs-only head must reacquire gates |
| Migration Plan | Characterized and listener-composable | Draft parent PR #7 composes the pg-erd route and response-policy contracts with an explicit normalized upstream-authority set. Only `backend` and `frontend` are admitted; unknown route targets fail closed. This does not widen `GatewayConfig` v1 |
| Migration Delivery | Bound and listener-composable | PR #10 requires exactly one explicit validated `UpstreamConfig` / Pingora peer for every identity admitted by an `EdgeMigrationPlan`, rejecting missing, duplicate, or undeclared concrete authority. PR #11 selects only those prevalidated peers. PR #12 keeps the exact authority bijection in Admin Config and materializes peers once before listener creation; no arbitrary per-request destination or service-discovery inference exists |
| Runtime Isolation | Declared-body and real-listener saturation/recovery predecessor GREEN | `RuntimeIsolationLimits` is shared by the generic and multi-route adapters. Non-zero body/in-flight limits are explicit; declared and streamed body overflow fail closed at HTTP 413 and exhausted admission at HTTP 503. The dedicated pg-erd process contract requires declared oversize rejection before origin delivery and now holds one routed request, proves a second routed request returns 503 before origin, then proves lease recovery with a later 200. Exact `850aff53...` passed the full test/coverage gate. Dedicated streamed/chunked body traffic remains a later-child acceptance item |
| Upstream TLS | Implemented generic candidate; pg-erd activation fail-closed contract added | Generic compiled local-CA/hostname verification proves custom trust and SNI mismatch behavior. The pg-erd Admin Config avoids trust preload and reads a custom bundle once during peer materialization; dedicated startup requires unreadable trust material to block listener activation. Pg-erd successful TLS/listener parity remains unproven and the captured legacy entryPoint is clear-text |
| HTTP protocol scope | Partial | Initial upstream adapter explicitly uses HTTP/1.1. No HTTP/2 or HTTP/3 parity claim exists without executable downstream/upstream contract evidence |
| Hop-by-hop / forwarding trust | Dedicated compiled-listener predecessor GREEN; docs-only head pending | `forwarding_policy` removes request-controlled `Forwarded`, `X-Forwarded-*`, `X-Real-IP` and legacy `X-Forwarded-Server`, then rebuilds characterized `X-Forwarded-For`, `X-Real-IP`, `X-Forwarded-Host`, `X-Forwarded-Port` and `X-Forwarded-Proto` from accepted downstream transport/request authority. The dedicated loopback contract supplies hostile proxy identity and requires direct loopback/Host-derived values with no attacker identity; exact `850aff53...` passed compile/test/coverage. The characterized Traefik entryPoint is clear-text `web`, so `http` is explicit; HTTPS needs its own TLS-derived scheme contract |
| Retry policy | Implemented, intentionally minimal | `max_retries=1` means one total upstream attempt and zero generic automatic retries; domain idempotency/replay policy stays with the product owner |
| Request limits | Partial | Declared and streamed/chunked body size plus process-wide in-flight backpressure are bounded. Configurable header, connection, per-route and origin-capacity budgets remain gaps |
| Failure recovery | Saturation/recovery executable; routed network failures remain later gaps | The dedicated pg-erd process contract proves fail-fast saturation before origin plus post-release capacity recovery at exact `850aff53...`. A generic compiled loopback contract separately requires a refused origin connection to return HTTP 502 within configured connection budget and proves `/readyz` remains healthy afterward. Dedicated pg-erd refusal/read-timeout/reset/partial-response/streaming cases remain later-child gaps at this dependency root |
| Health | Shared process boundary predecessor GREEN; docs-only head pending | `/livez` and `/readyz` are served locally through a shared internal Pingora process-health boundary in both adapters and bypass migration fallback routing. Consumer `/healthz` remains characterized application traffic to `backend`. The dedicated process contract explicitly distinguishes them and exact `850aff53...` passed compile/test/coverage; current documentation head must reacquire gates |
| Graceful drain | Shared server policy implemented; pg-erd process exit explicit in test harness | Both composition roots use shared explicit Pingora server policy: 5 s grace plus 10 s runtime shutdown timeout inside a 30 s external termination budget. Generic drain is tested. The pg-erd production-path fixture sends SIGTERM and waits for normal shutdown so serving-path coverage/evidence can flush; routed in-flight drain under active traffic remains to be proven separately |
| Logs / metrics / traces | Listener-capable candidate | Shared `observability` owns low-cardinality request/error/body/backpressure counters and credential/cookie-safe coarse logs. Both adapters delegate to it; paths, query strings, headers/cookies, credentials, customer payloads and product identifiers are excluded. Dedicated payload-free runtime assertions and tracing remain gaps |
| OCI isolation | Generic hardening implemented; dedicated invocation gap | Runtime is uid/gid 65532, read-only-root compatible, capability-free and `no-new-privileges`; base images are digest-pinned. #12 OCI evidence invokes the generic binary. Packaging and invoking `cwl-pingora-pg-erd-migration` under the same boundary must be proven before deployment; descendant #14 owns that bounded two-profile packaging slice |
| Dependency policy | Release-blocked | `.github#1605` owns the exact-release Pingora vs patched-`lru` decision and disposition of unmaintained `derivative 2.2.0` / `RUSTSEC-2024-0388`; `.github#810` independently owns the public non-fork Dependency Review compare-API HTTP 403 incident. Known-unsound downgrade, blanket advisory waiver, fail-open 403 handling, or substitute-scanner promotion is prohibited |
| Coverage / public API docs | Implementation predecessor 100% GREEN; docs-only head pending | Owned production line/region coverage remains required at 100%; `#![deny(missing_docs)]` and warning-denied rustdoc cover public APIs. Exact `850aff53...` passed the full coverage workload/export and unchanged 100% line/region enforcement after graceful child-process profile flush and validated-runtime-path repair. No source/file exclusion or threshold reduction was introduced; the documentation successor must reacquire exact-head evidence |
| Load / 20 ms p95 | Executable generic candidate only | Checksum-pinned k6 2.2.0 exercises 400 release-mode generic loopback requests across four VUs and gates p95 <20 ms with zero failures. This does not transfer to pg-erd multi-route serving. Representative routed concurrency, origin capacity and deployment/network measurements are required before 20 ms p95 becomes a pg-erd objective |
| Rollback | Documented, not rehearsed | Rehearsal requires an immutable protected release artifact/digest and a real consumer traffic transition |

## Organization edge inventory

Fresh organization code evidence finds no OpenResty usage. Responsibility class, not process name alone, determines migration scope.

| Repository / evidence | Classification | Migration consequence |
| --- | --- | --- |
| `linux-cluster-ops/docs/architecture/nginx-routing-inventory.md` plus Nginx/Certbot recovery evidence | ACTIVE_RUNTIME / CURRENT_OPERATOR_DOC | True shared-edge candidate, but current multi-vhost routing, static/PHP-FPM and certificate-adjacent operations exceed Pingora v1. Split authority and freeze executable traffic/TLS contracts first |
| `pg-erd-cloud/deploy/traefik/dynamic.yaml` at source commit `8dc746920c12988f082e914879d95e13c9693535` | ACTIVE_DEPLOYMENT / PLAUSIBLE_CONSUMER | Ordered exact `/healthz -> backend`, raw-prefix `/api -> backend`, fallback `/ -> frontend` plus four response-security fields. Consumer code also conditionally trusts sanitized `X-Forwarded-For`, so forwarding identity is migration behavior rather than cosmetic metadata. PRs #5/#6/#7/#10 characterize route/header/authority/peer binding, PR #11 composes them in Pingora callbacks, and PR #12 adds bounded Admin Config, dedicated process startup tests, shared process-health separation, real loopback backend/frontend traffic, declared-body rejection, and real-listener saturation/recovery. No terminal current-head pg-erd parity, shadow/canary or cutover is claimed yet |
| `naruon` NGINX ingress/live-E2E plus Traefik evaluation | ACTIVE_DEPLOYMENT / TEST_RUNTIME | Its Nginx proxy contract includes HTTP/1.1, long read/send timeouts, WebSocket Upgrade/Connection and forwarded identity semantics. Keycloak/authentication stays outside Pingora; only transport/edge policy can migrate after explicit parity evidence |
| `scopeweave`, `LineageWeave`, `inkspan` Nginx static-serving images/config | ACTIVE_STATIC_RUNTIME | Static hosting is not automatically a shared-edge migration; prove gateway responsibility before queueing |
| `life-os` ClusterIP-only base manifests with separately managed edge namespace | DELEGATED EDGE | Repository base manifests do not prove an embedded legacy edge to migrate |

No consumer is marked migrated, shadowed, canaried, cut over, or legacy-removed. Required sequence remains executable legacy characterization -> Pingora parity -> shadow/canary -> protected production cutover -> rollback evidence -> legacy removal.

## Context Graph and Enterprise Architecture dependencies — read only

`ContextualWisdomLab/context-graph-contracts` and `ContextualWisdomLab/enterprise-architecture-core` are not writable from this loop while the Context Fabric writer is active. This repository therefore does not duplicate their moving PR heads, base ancestry, checks, reviews, or branch-transition state. Every sweep re-reads all open PR/issues and records exact current evidence in `ContextualWisdomLab/.github#1608` for the sole owner path.

GREEN for an edge migration requires an immutable protected Context Graph release carrying canonical object/authority refs, truth status/origin, valid/system time, provenance, Context Assertion + CloudEvent schema/profile/AsyncAPI semantics, exact package identity, conformance/admission evidence, and consumer-verifiable release evidence binding exact package/bundle/SBOM bytes to protected source repository/ref/commit. A declaration-only source SHA is not release authority. Runtime request/log/customer data must not be copied into Context Graph authority.

The EA owner path must consume that released contract and project each approved edge migration as `current technology/interface -> migration initiative/scenario -> target technology/interface -> validated execution`, linking affected application/service/API, current and target provider/version, lifecycle, security/operability risk, accountable owner, dependency, canary/cutover/rollback state, and immutable Pingora artifact identity. It must preserve foreign authority and provenance, reject provisional PR heads, and prohibit cross-service application-table SQL.

## Dependency-ordered blockers

1. Reacquire exact-current-head CI, 100% owned production line/region coverage, rustdoc, k6, OCI, SAST and supply-chain evidence after every source or documentation movement; repair only evidence-backed repository defects. Organization runner acquisition is separately tracked in `.github#712`; queued jobs are not source GREEN.
2. Keep `.github#1605` and `.github#810` fail-closed until their respective policy and GitHub Dependency Review availability owner paths are resolved; do not suppress `RUSTSEC-2024-0388` generically.
3. Keep PRs #5, #6, #7, #10, #11 and #12 pre-traffic until exact-head quality/security evidence and coherent dependency ancestry are terminal. No child evidence repairs a parent release blocker.
4. PR #12 contains the bounded startup/Admin Config transition plus dedicated compiled-process route/header/body/saturation traffic. Historical Clippy, listener-authority, formatting and child-process coverage failures were repaired without threshold weakening. Exact implementation predecessor `079d6cad...` failed only at two Rust 1.98.0 formatting deltas in the new saturation/recovery test; formatter-only `850aff53...` then completed terminal GREEN CI `34159473276` (`load-contract 101858081643`, `oci-runtime 101858081794`, `test 101858081810`) and Supply Chain `34159473308` / `candidate-evidence 101858081874`, including strict lint/rustdoc and 100% owned-production line/region coverage. This baseline commit moves documentation, so current exact-head gates/review must be reacquired before #12 promotion. Descendants must ordinary/non-force adopt the stable repaired parent before their own evidence can become current.
5. After the basic listener contract is current-head GREEN, extend dedicated pg-erd acceptance with streamed body-limit behavior, unmatched-route/failure handling as applicable, payload-free observability assertions, timeout/reset/partial-response/streaming/network-failure recovery, routed graceful drain and representative routed concurrency/origin-capacity measurements before adopting a production 20 ms p95 objective. Real-listener in-flight saturation/recovery is already exercised at the #12 implementation predecessor and must be preserved through succession. A future HTTPS listener must separately prove TLS-derived forwarded scheme instead of reusing the current clear-text `web` assumption.
6. Package and invoke the dedicated migration binary under the same rootless/read-only-root/capability-free OCI boundary, then add a protected release path publishing an immutable image digest with SBOM/provenance/reproducibility evidence and rehearse rollback against that exact digest.
7. Satisfy then-live protected-branch review/governance without self-approval, bot-as-human claims, stale evidence transfer, or routine administrator bypass.
8. Wait for an immutable released Context Graph bundle with source-bound consumer-verifiable release evidence and a coherent compatible GREEN EA admission path before asserting authoritative architecture execution state.
9. Only then move `pg-erd-cloud` through explicit parity -> shadow/canary -> cutover -> rollback -> legacy removal. Other Nginx/Traefik surfaces remain separate responsibility-bound migration candidates and are not inherited automatically from this consumer profile.
