# Product / Technical Gap Baseline

This file is the code-current migration baseline for `ContextualWisdomLab/pingora-gateway`. Live PR metadata remains the exact-head authority; this document intentionally does not self-hash its own containing commit. Historical RED/GREEN detail remains in PRs, commits, workflow receipts, ADRs and `CHANGELOG.md` rather than being copied indefinitely into this snapshot.

## Authority and bounded contexts

`pingora-gateway` owns shared Ingress, Edge Routing, TLS consumption, HTTP Policy, Load Balancing mechanics, Observability, Admin Config and Runtime Isolation behavior that is genuinely edge/runtime responsibility. Product authentication and business logic remain product-owned. Keyverse remains identity authority; Wardnet/EgressWeave remain their policy authorities. The gateway consumes released/versioned contracts or explicit operator transport inputs and does not copy sibling source, issue cross-service SQL, or depend on mutable sibling PR heads.

Generic v1 remains a one-upstream Rust/Pingora process. The bounded `cwl-pingora-pg-erd-migration` process is a separate composition root for the characterized `pg-erd-cloud` route/header contract; it is not a general product-routing DSL.

## Dependency and promotion root

Foundation #1 remains `0da81a93f93e869c15bb7d34c55fc87479d16522`. Compiler prerequisite #56 remains Ready/open/mergeable at exact `18fb38b1ba70c4bf222642ef347f3d57a98379a2`; CI `33992794787` and Supply Chain `33992794799` are terminal GREEN, but an independent `APPROVED` review is still required before protected promotion. Bot/static review is not substituted for that governance gate.

Supplier-intake #54 remains Ready/open/mergeable at exact `50b0516a9249c4066e3a0f305dbf2759eae3ae06`. Its hosted RED is intentional: committed `Cargo.lock` still contains `derivative 2.2.0`, and `tests/supply_chain_policy.rs` rejects that exact package under the CWL release criterion. Audit ignores, lock deletion, mutable supplier pins, muted regressions and scanner suppression are not admissible repairs.

Supplier-semantics #62 remains Ready/open/mergeable at exact `32e0aeedac7b0fe6234d476245f37994b1b9168f`, with exact CI `34051494660` and Supply Chain `34051494627` terminal GREEN. Its Rust measured-origin receipt records 400 requests, 800/800 status/body checks, zero HTTP failures and p95 `0.71593235 ms`; it also preserves the required `PeerOptions` Debug and `Backend` equality/hash/order semantics that an upstream repair must not regress.

Fresh supplier authority remains protected `cloudflare/pingora/main@09696b51bc59315353d96686355861604d0bb48c`. The accepted next step is a maintainer-integrated, release-qualified immutable Pingora repair removing `derivative` from the relevant workspace/core/load-balancing manifests and regenerated lock while preserving #62 semantics. Gateway promotion then requires an exact supplier bump, committed lock regeneration, unchanged #54 GREEN, preserved #62 GREEN, and #56 independent approval/governance.

## Current pg-erd stack

Parent #12 is exact `69f22265cd88881b8e14cedb32defa0a91180aa2` and is terminal hosted GREEN: CI `34161291459` and Supply Chain `34161291354` passed formatting, compile/test, strict Clippy, warning-denied rustdoc, 100% owned-production line/region coverage, dependency-lock verification, generic load and OCI runtime. Its public `PgErdMigrationConfig::build_proxy()` boundary revalidates deterministic Admin Config invariants so direct Serde deserialization cannot bypass version, listener authority, runtime, keepalive or transport-authority checks.

`#14` is exact parented on #12 and reached exact `8937364909b82f50fd911aa001a8d073b517f5d9` through ordinary non-force adoption. CI `34163230667` is terminal GREEN for load, test and dual-profile OCI runtime; Supply Chain `34163230576` is terminal GREEN. Generic k6 artifact `10033390215` records 400 requests, 800/800 checks, zero HTTP failures and p95 `1.5951211 ms` against the unchanged `<20 ms` loopback threshold. This is controlled generic loopback evidence, not routed pg-erd or representative deployment SLO evidence. OCI proves both admitted image profiles under uid/gid 65532, read-only root, dropped capabilities and `no-new-privileges`; Supply Chain builds/scans both profiles, produces SPDX dependency SBOM evidence and binds receipts to exact source. Registry-bound immutable image identity and release-bound provenance remain unproven.

`#14`'s shared `edge_contract` is the sole effective socket-authority implementation. Traffic/metrics overlap rejects port zero, same-family wildcard/concrete aliases, IPv6-wildcard/IPv4 dual-stack ambiguity, native↔mapped IPv4 aliases, native/mapped wildcard aliases and mapped-to-mapped wildcard aliases. `migration_admin` reuses that boundary instead of duplicating it.

`#15` preserves the generic forwarding-trust delta while adopting current #14 without force-push or destructive rebase. Its exact current head is `bb65f2810b178bda46ed7eb5c6a62aae9fe36403`. The effective forwarding invariant removes request-controlled `Forwarded`, `X-Forwarded-For`, `X-Forwarded-Host`, `X-Forwarded-Port`, `X-Forwarded-Proto`, `X-Forwarded-Server` and `X-Real-IP`, then emits only gateway-owned `Forwarded: proto=http`; generic v1 makes no client-IP or trusted-proxy provenance claim.

`#15` exact CI `34166547052` and Supply Chain `34166547010` are terminal GREEN after an executed coverage RED identified one impossible error-propagation branch for the static gateway-owned `Forwarded` literal. Minimum repair made that literal an explicit invariant without changing routing, sanitizer output or client-visible behavior. Current CI passed formatting, locked compile/test, strict Clippy, warning-denied public rustdoc, 100% owned-production line/region coverage, dependency-lock verification, generic load and dual-profile OCI runtime. Load artifact `10034472458` records 400 requests, 800/800 checks, zero HTTP failures and p95 `1.31409845 ms`. Fresh exact-range technical review reports no actionable finding; it is not independent human approval.

`#16` is the runtime-isolation traffic child on exact #15. Its exact current head is `356f3f250043d71c9bb1c0655481cd6984f3ae57`. `tests/pg_erd_runtime_isolation_traffic.rs` drives the compiled `cwl-pingora-pg-erd-migration` process over real loopback listeners. A 9-byte chunked body against an 8-byte budget must return 413 while `/readyz` remains 200. With `max_in_flight_requests=1`, one routed backend request holds the only admission lease; the next routed request must return 503 in `<1 s`, materially before the configured 2 s upstream read budget; `/readyz` remains 200; metrics must expose the exact Prometheus sample `cwl_pingora_gateway_backpressure_rejections_total 1`; after releasing the held request, a later routed request must return 200. Exact-line matching prevents samples such as `10` or `11` from false-passing.

`#16` exact CI `34168471195` is terminal GREEN for load, test and OCI runtime. The exact k6 artifact records 400 requests, 800/800 checks, zero HTTP failures and p95 about `0.919 ms` against the unchanged `<20 ms` loopback threshold. Supply Chain `34168471176` is also terminal GREEN: committed dependency audit, both admitted candidate-image builds, SPDX dependency SBOM generation, generic and pg-erd vulnerability scans, exact-source binding and success evidence upload all completed successfully. Fresh exact-range CodeRabbit review of `bb65f281...356f3f25` reports no actionable issue and confirms the prior fail-fast finding and metric-oracle ambiguity are closed. No predecessor receipt is transferred beyond this exact head.

`#17` is the deterministic refused-origin failure child on exact #16. The source binds the configured IPv4 backend address in the test process without calling `listen(2)`, retains exclusive ownership through the failure request, and first requires direct `TcpStream::connect_timeout` to return `ErrorKind::ConnectionRefused`. Gateway traffic must then return 502 within a conservative one-second envelope around configured `connection_ms=200` / `total_connection_ms=400`, keep `/readyz` 200, expose exact Prometheus sample `cwl_pingora_gateway_request_errors_total 1`, and prove a later independent fallback route to `frontend` returns 200. This is an end-to-end fail-closed envelope, not a claim that either configured connection timeout elapsed.

`#17` exact current head `7a7e1f1ca4c8310220b7ff2fb96e01027a7e89f3` is independently terminal hosted GREEN. CI `34174138725` completed success for load, test and dual-profile OCI runtime; Supply Chain `34174138778` / `candidate-evidence 101900082272` completed success through committed dependency audit, both candidate-image builds, SPDX dependency SBOM, both image scans, exact-source binding and success-evidence upload. Fresh exact-range CodeRabbit re-review of `356f3f25...7a7e1f1` reports no actionable findings and confirms the effective delta remains four paths. Bot/static review is not independent human approval.

`#18` owns the next distinct connected-silent-origin failure phase. Its retained child contract starts a real characterized backend, confirms `/api/read-stall` reached that authority, and holds the accepted connection open without sending response bytes until after the gateway has already produced the downstream failure. With `read_ms=100`, acceptance requires HTTP 502 inside a conservative one-second outer envelope, `/readyz` 200, exact Prometheus sample `cwl_pingora_gateway_request_errors_total 1`, and a later independent `frontend` request 200. Pingora `read_timeout` is treated only as a per-read inactivity budget; the contract deliberately does not claim a whole-response lifetime.

Ordinary two-parent commit `0c90ff2a91bda6c310cd01bded135c89f7d21c87` adopted exact current #17 `7a7e1f1...` as second parent while retaining predecessor #18 `c0c160ac1b4811f0f34182edb3419c3a3766476d` as first parent. Fresh compare from current #17 remains behind=0 with merge base exactly `7a7e1f1...` and exactly six effective child paths: `API_CONFIG_CONTRACT.md`, `CHANGELOG.md`, `TEST_STRATEGY.md`, `docs/doctoring/TRACEABILITY.md`, this baseline, and `tests/pg_erd_read_stall_traffic.rs`.

The first current-parent exact head `c7735ca8067e77c588a474cc41df3ce47bd04d43` completed CI `34175094225` and Supply Chain `34175094216` GREEN. Fresh fixture review then found one owner-local authority flake: traffic and metrics addresses were selected through sequential bind-and-drop ephemeral reservations, allowing the first address to be reused before the second selection. Test-only commit `b31c85ba204f8699ea70396fbf51cb60fbf8f4c0` instead holds both reservations concurrently, reads both addresses while both sockets are held, asserts they differ, and only then releases them for the compiled process.

Exact follow-up `9bcf3df639c633fdf9f8a985c6fde307d09189ce` independently reacquired terminal CI `34176646015` and Supply Chain `34176646009` GREEN. Test, strict lint, warning-denied public rustdoc, 100% owned-production line/region coverage, dependency-lock verification and dual-profile OCI runtime all passed. Exact generic-loopback artifact `10037539770`, digest `sha256:c7787c3943292fd4769315d0d6fc088ee20a5ca1d6c91c35417bc6107892a327`, records 400 requests, 800/800 checks, zero HTTP failures and p95 `0.9491437 ms`. Fresh exact-range CodeRabbit review of `7a7e1f1...9bcf3df` generated no actionable comments and covered exactly the six declared child files, but its pre-merge docstring check reported 11.11% across nine touched test functions against an 80% service threshold.

That documentation warning is treated as a repair finding rather than ignored because the migration lane requires complete owned documentation. The current branch therefore adds behavior-neutral rustdoc to all nine touched test functions/helpers, including teardown semantics, simultaneous listener reservation, startup/listener proof, raw request capture, header-only origin reading and the final connected-inactivity contract. This baseline projection accompanies that documentation-only change. Neither change alters production Rust, routing, timeouts, metrics, traffic volume, OCI policy, dependency pins or product/security authority. Because the exact source head has moved past `9bcf3df...`, the prior hosted receipts and technical review remain historical evidence only; the resulting exact head must reacquire hosted CI/Supply Chain and fresh exact-range technical review before #19 parent succession.

## Capability state and buyer-visible gaps

| Area | Current state | Remaining acceptance |
| --- | --- | --- |
| Admin Config / network authority | Implemented; #12/#14 exact hosted GREEN; #18 fixture now structurally reserves distinct listeners | Revalidate on the resulting #18 exact head and every descendant restack |
| Generic forwarding trust | #15 exact source/review/CI/Supply Chain GREEN and inherited through the current parent stack | Preserve the exact invariant through descendants; no client-IP/trusted-proxy claim until separately characterized |
| Edge routing / HTTP policy | Characterized and compiled for pg-erd | Routed production-process parity must remain GREEN through descendant restacks |
| Runtime isolation | Declared/streamed body and in-flight limits implemented; #16 exact real-listener acceptance is hosted GREEN | Preserve through #17/#18+; origin-capacity semantics and SIGTERM drain remain later pg-erd acceptance |
| Failure recovery | #17 refused-origin exact hosted/technical GREEN; #18 connected-silent behavior reached exact hosted/technical GREEN before the documentation-only review repair | Reacquire exact #18 hosted/review on the documentation-complete head; reset, post-commit truncation and slow-drip/streaming lifetime remain unproven |
| Upstream TLS | Generic local-CA/SNI verification exists; pg-erd fail-closed trust activation exists | Successful pg-erd TLS listener/origin path and representative TLS performance remain unproven |
| Protocols | HTTP/1.1 path exists | Downstream TLS/H2, H2→H1 Cookie behavior, WebSocket/Extended CONNECT and explicit H3/QUIC disposition require separate supplier-capable contracts |
| OCI / supply chain | #14/#15/#16/#17 and #18 predecessor dual-profile candidate build, least privilege, audit, SPDX SBOM and scan evidence GREEN | Reacquire on the resulting #18 exact head; immutable registry digest, signing/attestation/provenance, release-bound SBOM, reproducibility receipt and rollback rehearsal |
| Performance | #18 predecessor generic loopback p95 `0.9491437 ms`; supplier measured-origin #62 p95 `0.71593235 ms` | Do not transfer predecessor results or treat controlled loopback as production SLO proof; routed pg-erd concurrency/TLS/failure measurements remain required |
| Observability | Low-cardinality counters/logging and separate metrics listener; #16 exact single-rejection and #17/#18 predecessor exact single-error telemetry GREEN | Revalidate exact single-error behavior after the documentation-only head move; broader payload-free runtime assertions and tracing remain gaps |
| Documentation / review | All nine #18 touched test functions/helpers now carry purpose/constraint rustdoc in source | Reacquire current-head hosted checks and fresh exact-range review; do not transfer the 9bcf review |
| Release / migration | No protected release or consumer cutover credit | Immutable release → parity → shadow/canary → rollback rehearsal → cutover → verified Nginx/OpenResty/legacy removal |

## Execution order

The current dependency order is `#54 derivative RED + #62 exact semantics/load GREEN → maintainer-integrated immutable supplier repair → gateway supplier bump and committed lock regeneration → #54 GREEN + preserved #62 GREEN → #56 independent APPROVED/governance → foundation/protected integration → #12 → #14 → #15 → #16 exact hosted GREEN → #17 deterministic refused-origin exact hosted/technical GREEN → #18 documentation-complete resulting exact hosted GREEN/review → #19/#20/#21 and later descendants parent-first ordinary non-force succession → remaining routed TLS/failure/drain/protocol acceptance → immutable gateway release/SBOM/provenance/reproducibility/rollback → shadow/canary → cutover → verified legacy removal`.

No Draft state, predecessor receipt, bot review, local image ID, mutable supplier PR, queue state or controlled loopback measurement is treated as protected merge, release, canary, cutover, rollback or legacy-removal evidence.