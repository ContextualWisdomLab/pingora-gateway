# Product / Technical Gap Baseline

This file is the code-current migration baseline for `ContextualWisdomLab/pingora-gateway`. Live protected-branch, PR, review, workflow, release and supplier metadata remain the exact authority. Historical RED/GREEN detail belongs in PRs, commits, workflow receipts, ADRs and `CHANGELOG.md`; this snapshot keeps durable architecture, acceptance and remaining buyer-visible gaps. Mutable current-head SHAs and in-flight run IDs are deliberately not copied here because an evidence-refresh commit would immediately stale its own statement.

## Authority and bounded contexts

`pingora-gateway` owns shared Ingress, Edge Routing, TLS consumption, HTTP Policy, Load Balancing mechanics, Observability, Admin Config and Runtime Isolation behavior that is genuinely edge/runtime responsibility. Product authentication and business logic remain product-owned. Keyverse remains identity authority; Wardnet/EgressWeave remain their policy authorities. The gateway consumes released/versioned contracts or explicit operator transport inputs and does not copy sibling source, issue cross-service SQL, or depend on mutable sibling PR heads.

Generic v1 remains a one-upstream Rust/Pingora process. The bounded `cwl-pingora-pg-erd-migration` process is a separate composition root for the characterized `pg-erd-cloud` route/header contract; it is not a general product-routing DSL. `PgErdMigrationConfig` deliberately denies unknown fields and admits only concrete listener, runtime budget and characterized `backend`/`frontend` transport bindings. Route authority stays compiled into the migration plan rather than becoming operator-configurable product routing. Pg-erd configuration version 2 adds only an explicit Runtime Isolation response-body progress lifetime; version 1 and generic v1 do not silently acquire that semantic.

## Dependency and promotion root

Foundation #1 and compiler prerequisite #56 remain the earlier promotion root. #56 has terminal CI/Supply Chain evidence but protected promotion still requires an independent `APPROVED` review; owner or bot technical comments are not substituted for that gate.

Supplier-intake #54 remains intentional hosted RED because the committed gateway dependency graph still contains unmaintained `derivative 2.2.0` / `RUSTSEC-2024-0388`. Audit ignores, lock deletion, mutable supplier pins, scanner suppression and muted regressions are not admissible repairs. Supplier-semantics #62 remains exact hosted/technical GREEN for the required `PeerOptions` Debug surface, `Backend` equality/hash/order semantics and bounded Rust-origin load contract.

Pingora 0.9.0 is now published and contains the upstream graceful-shutdown lost-wakeup/sharded-notification successor, but publication does not close unrelated supplier roots: its released workspace still contains `derivative 2.2.0`, HTTP/1 parser byte/count controls and a monotonic whole-request-header deadline remain absent, and the mixed-protocol Cookie/body-framing roots remain separate. GitHub reports the 0.9.0 Release object as mutable (`immutable:false`), so CWL still requires exact commit/package provenance and unchanged downstream executable acceptance before promotion.

The supply-chain promotion path remains: maintainer-integrated, release-qualified Pingora repair removing `derivative` from the relevant workspace/core/load-balancing manifests and regenerated lock while preserving #62 semantics → exact gateway supplier bump and committed lock regeneration → unchanged #54 absence regression GREEN and preserved #62 GREEN → #56 independent approval/governance → protected foundation promotion.

## Current pg-erd stack

The parent chain through #33 is ordinary/non-force and has exact hosted technical evidence at each retained final head. Those contracts cover bounded Admin Config/socket authority, forwarding-trust reconstruction, body/in-flight isolation, refused and connected-silent origins, post-header inactivity, OCI metrics identity, routed SIGTERM drain, routed latency, payload-free observability, pre-/post-commit TCP reset phases, recursive gateway network-authority separation, fail-closed HTTP/1 protocol-transition admission, process-wide Pingora-family diagnostic redaction and defense-in-depth denial of HTTP/1 Upgrade at immutable peer construction. Predecessor receipts are never transferred to changed descendants.

### `#21` post-header partial-response phase

This phase owns the characterized backend contract that declares `Content-Length: 20`, commits only the seven-byte `partial` prefix, waits until that prefix has been observed downstream, then closes. Acceptance preserves the committed 200/framing rather than inventing a second status or failover, requires `/readyz` 200, an exact single request-error Prometheus sample, and an independent `frontend` recovery request. The framing oracle parses field lines semantically, matches `Content-Length` case-insensitively, trims field-value whitespace, requires exactly one value equal to `20`, and rejects lookalike or duplicate/conflicting fields. #21 has terminal exact-head CI/Supply Chain evidence; descendants must independently revalidate.

### `#22` routed concurrency/latency acceptance

This phase has closed its unchanged exact-head hosted and changed-range technical-review gates. Its measured origin is Rust-only: `tests/load/load_origin.rs` is a bounded std-only HTTP/1.1 loopback origin with finite worker and queue budgets, a 64 KiB request-header cap, deterministic Content-Length framing, startup validation and direct fixture tests. Routed k6 uses four VUs and 400 total iterations, alternates characterized `/api/load-contract` and `/load-contract`, requires exact 200/body identity and zero HTTP failures, independently gates aggregate/backend/frontend p95 below 20 ms, and requires at least 198 samples per route. This remains controlled loopback evidence, not representative deployment, TLS, multi-hop or origin-capacity SLO proof.

### `#23` payload-free shared-observability acceptance

This phase was ordinarily/non-force restacked on the final #22 tree and has independently closed exact-head hosted CI/Supply Chain plus current-range technical review. `tests/pg_erd_payload_free_observability.rs` sends a real routed request carrying unique URI/query, Host, Authorization, Cookie and product-context sentinels. The backend must receive the exact case-sensitive request target and semantically exact HTTP fields, while shared gateway stderr must emit only the exact bounded completion message and none of the sentinels. Reliability oracles keep traffic/metrics reservations concurrent, bound origin request reads to five seconds/64 KiB, reject `X-Forwarded-Host` as a substitute for `Host`, and require exact Prometheus sample-line equality. Bot/static technical review remains technical evidence only and does not replace #56 independent human approval.

### `#24` pre-header upstream TCP reset

This phase owns the distinct case where the characterized backend connection succeeds, the complete routed request reaches `backend`, and that backend then performs an abortive Linux `SO_LINGER(0)` close before sending any response header. It was ordinary/non-force restacked from the final #23 tree while preserving only this valid child test delta and code-current documentation; no stale parent blobs or predecessor receipts were replayed.

`tests/pg_erd_upstream_reset_traffic.rs` requires HTTP 502 within two seconds despite `read_ms=5000`, no silent frontend failover, `/readyz` 200, exactly one `cwl_pingora_gateway_request_errors_total 1` sample, and a later independent frontend HTTP 200. Traffic and metrics listener reservations remain simultaneous until process startup; origin header reads fail closed after five seconds or 64 KiB; exact Prometheus sample-line matching rejects numeric-prefix values such as `10`; exact HTTP/1.1 status parsing rejects protocol-case and numeric-prefix lookalikes. This phase has closed unchanged-head CI/Supply Chain and a fresh exact-head technical review. Bot/owner technical evidence does not replace #56 independent human approval.

### `#25` post-commit upstream TCP reset

This phase owns the later transport case where the characterized backend first commits HTTP 200 with exactly one `Content-Length: 20` field and the seven-byte `partial` body prefix, waits until the downstream has actually observed that committed header and prefix, then applies Linux `SO_LINGER(0)` and aborts the established connection. Ordinary/non-force succession starts from the final #24 tree and preserves the historical child as ancestry while replaying only the valid post-commit-reset delta.

`tests/pg_erd_post_commit_reset_traffic.rs` preserves the already-committed 200 and exact framing, terminates before the declared 20-byte body completes, avoids a fabricated second status or silent failover, keeps `/readyz` 200, exposes exactly one low-cardinality request-error sample, and allows an independent frontend recovery request. Listener reservations are concurrent, origin header reads are bounded to five seconds/64 KiB, `Content-Length` is matched by exact field identity so `X-Content-Length` cannot satisfy the oracle, Prometheus samples use exact whole-line equality, and response status uses an exact HTTP/1.1 three-digit parser. Final #25 has terminal exact-head CI/Supply Chain; a fresh owner technical sweep found no new actionable source/documentation defect. The included CodeRabbit review was temporarily rate-limited, so no bot review credit is inferred or transferred.

### `#27` recursive gateway network-authority separation

This phase prevents operator-configured upstream transport authority from aliasing either gateway-owned listener. Generic `GatewayConfig` and bounded `PgErdMigrationConfig` reuse one `socket_authorities_overlap` model for exact addresses, IPv4/IPv6 wildcard aliases, IPv4-mapped IPv6 aliases and the conservative IPv6-wildcard/IPv4 dual-stack case. An upstream that aliases the public traffic listener fails with `UpstreamListenerCollision`; one that aliases the internal metrics listener fails with `UpstreamMetricsListenerCollision`. Distinct concrete IP authorities on the same port remain configurable, so the invariant prevents recursive self-proxying and accidental metrics exposure without becoming product routing policy.

The child ordinarily/non-force adopted final #25 with a two-parent ancestry commit whose resolution tree was exactly the final #25 tree, then reapplied only the shared production invariant, bounded migration-admin use of that invariant, and dedicated current-parent acceptance. `tests/network_authority_self_loop_contract.rs` proves generic and pg-erd exact/wildcard/dual-stack collision rejection, distinct-concrete same-port admission, public `build_proxy` revalidation after direct deserialization, and compiled fail-closed startup for both composition roots. The final #27 head has terminal CI/Supply Chain and a fresh technical review with no actionable source/documentation defect; descendants must independently revalidate rather than transfer those receipts.

### `#29` HTTP/1 protocol-transition admission

WebSocket and other connection-wide HTTP/1 Upgrade semantics are not admitted by generic v1 or the bounded pg-erd contract. Final #29 is ordinarily/non-force based on final #27. A transport-neutral `protocol_transition_policy` treats either an `Upgrade` field or a case-insensitive comma-delimited `upgrade` token in any `Connection` field value as an attempted protocol transition, while rejecting false matches such as `x-upgrade`.

Both Pingora composition roots call the shared delivery guard after gateway-local health handling but before request admission and any route/upstream selection; that is the source-ordering contract. The admitted v1 behavior is HTTP 501 fail-closed, not partial WebSocket support. `tests/protocol_transition_traffic.rs` exercises both compiled listeners with WebSocket-shaped HTTP/1.1 requests, requires exact 501, then observes each dedicated origin listener for a fixed 500 ms post-response window and fails if any connection appears during that window; `/readyz` must remain 200. The 500 ms oracle is deliberately bounded evidence and is not described as proof that an arbitrarily delayed origin connection could never occur.

Final #29 closed exact-head CI, Supply Chain and a fresh exact-current owner technical review after formatter-only and documentation overclaim repairs. Its technical COMMENT is not an independent `APPROVED` governance credit. Descendants may ordinarily/non-force adopt that final tree but must independently reacquire checks/review after any source or documentation movement.

### `#31` process-wide payload-safe dependency logging

The shared `observability` target is already payload-minimized, but pinned Pingora trace/debug paths can format request-derived headers or serialized request headers before CWL-owned completion logging. Both production composition roots therefore install one shared `logging_policy` before configuration or listener activation. Operator `RUST_LOG` still selects levels and targets, while Pingora-family dependency record message bodies are replaced with one static marker before `env_logger` formats them. Product/application loggers and richer product diagnostics remain outside this gateway authority.

The ordinary/non-force succession adopted final #29 as a two-parent resolution tree before reapplying only the still-valid logging source, both composition-root wiring changes and compiled acceptance. `tests/pingora_diagnostic_log_safety.rs` keeps traffic/metrics reservations simultaneous, bounds origin accept/header acquisition to five seconds and 64 KiB, proves exact URI/Host/Authorization/Cookie sentinels reach the origin, snapshots Pingora redaction-marker activity after readiness probes, requires a new redacted supplier diagnostic after the characterized secret-bearing request, and then requires no sentinel anywhere in process stderr. The completion-log oracle uses an exact line suffix so numeric-prefix lookalikes do not manufacture GREEN. Final #31 has terminal exact-head CI/Supply Chain and an exact-head owner technical COMMENT reporting no actionable source/test/documentation finding; that technical evidence is not an independent human approval.

### `#33` immutable peer HTTP/1 Upgrade denial

Request-level rejection alone leaves a transport-policy mismatch if the immutable Pingora peer retains the supplier `HttpUpstreamRequestPolicy::standard()` behavior, whose current pinned semantics admit WebSocket upgrades. Final #33 ordinarily/non-force adopts final #31 as its resolution tree while preserving the historical #33 ancestry, then reapplies only the valid defense-in-depth delta: `pingora_delivery::build_peer_from_validated` sets `HttpUpstreamRequestPolicy::deny_upgrades()`, `tests/peer_protocol_policy.rs` locks that exact peer option, and Proposed ADR 0009 records the choice and supplier boundary.

The peer setting preserves standard hop-by-hop and `Connection`-nomination sanitization while denying HTTP/1 Upgrade underneath the existing source-ordering/501 admission guard. It does not implement WebSocket, CONNECT, product auth/business routing, retries, Wardnet/EgressWeave policy, or Keyverse identity. Protected supplier `main` remains pinned as dependency authority; the current public WebSocket race issue and proposed repair remain owner-path evidence only, not mutable dependencies. Final #33 has independently closed exact-head CI/Supply Chain and current-range owner technical review; that COMMENT evidence is not an independent human `APPROVED` governance decision.

### `#37` pg-erd upstream TLS trust and hostname acceptance

The bounded pg-erd composition root independently proves the TLS contract already characterized by generic v1 rather than inheriting generic evidence. Final #37 ordinarily/non-force adopts final #33 and changes only its dedicated TLS acceptance plus code-current documentation. `tests/pg_erd_local_ca_tls.rs` generates a short-lived local CA and `backend.test` certificate at test time, configures an explicit trust bundle and SNI for the characterized `backend`, and keeps `frontend` as the separate clear-text authority.

Matching CA plus SNI carries `/api/tls` through the real migration listener to the TLS backend and preserves an independent fallback request. Reusing the valid CA with a mismatched SNI fails the backend route as exact HTTP 502 without route failover, keeps `/readyz` HTTP 200, sends zero HTTP request bytes to the TLS backend after the failed hostname check, and preserves an independent frontend recovery request. Traffic/metrics reservations remain simultaneous until startup, accepted sockets carry five-second I/O deadlines, request headers are capped at 64 KiB, and exact HTTP/1.1 three-digit status parsing rejects protocol-case and numeric-prefix lookalikes. This is upstream TLS trust/hostname evidence only; downstream TLS termination, certificate issuance/rotation authority and representative TLS performance remain separate gaps. Final #37 has terminal unchanged-head CI/Supply Chain and current-range technical COMMENT evidence; that does not replace #56 independent human approval.

### `#39` pg-erd response-body progress lifetime

This phase ordinarily/non-force adopts final #37 while preserving the historical response-lifetime child as ancestry and reapplying only the valid versioned Runtime Isolation delta. Pg-erd version 2 requires a positive `max_upstream_response_body_ms`; version 1 rejects that field and generic v1 remains unchanged. The budget starts at the first non-informational upstream response header and cannot be reset by later headers. Runtime enforcement checks elapsed monotonic time only on non-empty upstream body progress, so empty/end-of-stream bookkeeping callbacks cannot manufacture a timeout. A non-empty progress callback at or beyond the configured budget becomes an upstream-scoped fatal error. If status/framing is already committed, the gateway terminates the incomplete response rather than inventing a second status, retry or failover.

`tests/pg_erd_slow_drip_response_traffic.rs` separates this control from Pingora `read_ms`: the characterized backend delays 150 ms before committing HTTP 200 with `Content-Length: 20`, then writes one byte every 60 ms while `read_ms` is 500 ms and version 2 declares a 300 ms body-progress lifetime. Acceptance requires the downstream to survive at least the header-delay plus body-lifetime lower bound, terminate before the full 20-byte drip can complete, preserve exact committed 200/framing, record exact request-error telemetry, keep `/readyz` healthy, and permit later independent frontend recovery. Traffic/metrics reservations are simultaneous; origin reads are bounded to five seconds/64 KiB; HTTP status, `Content-Length`, and Prometheus metric oracles reject protocol/name/numeric-prefix lookalikes. This is deliberately a progress-driven bound, not an exact timer interrupt of a currently pending read. Incomplete response-header slow delivery and broader admitted long-lived-stream semantics remain separate gaps. Final #39 has terminal unchanged-head CI/Supply Chain and current-range technical evidence. ADR 0010 remains Proposed pending the broader protected promotion/governance path.

### `#42` bounded-origin capacity acceptance

This phase ordinarily/non-force adopts final #39 while preserving the historical capacity child as ancestry. The historical `tests/load/load_origin.rs` capacity work is already byte-identical to final #39, so the restack adopts it rather than replaying a stale parent blob. The unique capacity overlay is the dedicated workflow, `tests/load/pg_erd_gateway_capacity.js`, `tests/load/run_pg_erd_capacity.sh`, and `tests/pg_erd_capacity_toolchain_contract.rs`; `CHANGELOG.md`, `TEST_STRATEGY.md`, and this baseline project the durable contract on the parent-correct branch.

The capacity contract is separate from #22's four-VU regression. A one-worker Rust-origin self-check uses `Connection: close` and 150 ms service delay to prove two simultaneously admitted direct-origin requests serialize before measurement. Measured traffic starts four-worker `backend` and `frontend` origins with `Connection: close` and 1 ms service delay, then sends 1600 iterations across 16 VUs through the exact release-mode pg-erd binary. It requires exact route-specific 200/body identity, zero failed checks/HTTP requests, aggregate/backend/frontend p95 below 20 ms, and at least 792 requests per route. The route floor admits at most one alternation imbalance per VU; no application route warm-up is allowed. The dedicated lane installs, selects, and verifies Rust 1.98.1 before the release-mode candidate build; its narrow toolchain regression rejects Rust 1.98.0 and alternate workflow selectors so capacity evidence cannot be produced by the compiler version repaired by 1.98.1. This complements rather than replaces compiler prerequisite #56. Final #42 has terminal exact-head CI, Supply Chain, dedicated bounded-origin capacity execution, resolved inline review threads and current-range technical evidence. Its controlled loopback result remains origin-capacity/connection-churn regression evidence rather than representative TLS/network/multi-hop/container-orchestrator or production SLO proof; descendants must independently revalidate rather than transfer those receipts.

### `#44` supplier-bound HTTP/1 header-control projection

This documentation-only phase ordinarily/non-force adopts final #42 with the historical #44 child preserved as first-parent ancestry and a resolution tree taken from final #42. It does not copy stale parent blobs or change production Rust. The projection keeps three supplier-bound controls distinct: #43 / `cloudflare/pingora#993` is downstream HTTP/1 parser byte/count admission; #45 / `cloudflare/pingora#447` is downstream HTTP/1 whole-request-header lifetime; #40 / `cloudflare/pingora#992` is upstream incomplete response-header lifetime.

Pinned Pingora has finite HTTP/1 parser ceilings, but they are supplier-fixed rather than an operator-controlled CWL parser-phase budget. A `ProxyHttp` callback runs after header construction and cannot be credited as pre-allocation admission. Likewise, downstream `read_timeout` is a per-read inactivity budget rather than a monotonic whole-header deadline, so successful partial reads can keep an incomplete header alive across repeated relative waits. HTTP/2 decoded-header-list accounting remains a separate protocol contract. No speculative Admin Config field, callback watchdog, retained Nginx/Traefik front proxy, timeout reinterpretation or mutable supplier fork is credited as closure. Final #44 independently closed exact-head CI, Supply Chain, bounded-origin capacity and current-range technical evidence on its unchanged final head; those receipts do not transfer to changed descendants.

### `#47` supplier-bound parked-read graceful-shutdown projection

This documentation-only successor ordinarily follows final #44 and does not copy Pingora proxy internals or consume a mutable contributor branch. It keeps #46 separate from the three header controls above and from #20's already-admitted in-flight SIGTERM drain proof. Final #47 has independently closed its unchanged-head CI, Supply Chain, bounded-origin capacity and technical review gates; that closes the documentation projection only, not the supplier/runtime defect.

The gateway's admitted Pingora revision still has the single-`Notify` lost-wakeup behavior characterized by #47/#70, but public supplier authority has advanced: Pingora 0.9.0 is published from the protected commit containing the maintainer-integrated `ShardedNotify` successor, configured-service-thread shard sizing and shutdown-state handshake. Historical #844/#969 remain provenance for the correctness/performance work rather than an integration prerequisite. The GitHub 0.9.0 Release object is reported as `immutable:false`, so publication advances #46 to release qualification rather than automatically authorizing the gateway dependency.

Issue #46 therefore remains an explicit Runtime Isolation / operability supplier gap until CWL qualifies an exact immutable/provenance-bound supplier identity, performs an ordinary pin/lock update, and the unchanged #70 process acceptance turns GREEN. ADR 0011 remains Proposed. The many-core contention/profile half remains separate from correctness.

### `#70` real-process parked-read lost-wakeup acceptance

#70 is the executable successor to final #47 and keeps production gateway Rust, configuration, routing, retry policy, product auth/business logic, Keyverse/Wardnet/EgressWeave authority and the currently admitted Pingora supplier pin unchanged. Its process-level fixtures independently cover generic v1 and the bounded pg-erd composition root. Each preserves the existing invariant that an already-admitted request can complete during SIGTERM drain. The causal barrier additionally establishes a separate reusable `/livez` sentinel before SIGTERM and parks it on a partial next HTTP/1 request. After SIGTERM stops new accepts, that preexisting sentinel must observe EOF inside the one-second pre-fallback evidence window; only then is the held admitted origin response released. The subject must complete its `Connection: keep-alive` body and its newly created next keep-alive read must also observe EOF while `signal_sent_at` remains strictly inside the production five-second grace period.

The origin-response release channel has a separate 30-second watchdog using the already-governed external termination budget. That watchdog is only a leak/hang safeguard and deliberately does not define graceful-shutdown semantics: it starts before SIGTERM, whereas all product acceptance remains anchored to `signal_sent_at`, the unchanged one-second EOF window and the five-second production grace. This prevents pre-signal runner scheduling delay from manufacturing a false RED without weakening the shutdown oracle.

The currently admitted older Pingora revision fails this deterministic post-notification park on the generic and bounded pg-erd compiled processes. The failure is intentionally retained as supplier RED rather than hidden by shortening grace, disabling keep-alive, reducing worker semantics, vendoring mutable supplier code or weakening the close oracle. Canonical CI uses `cargo test --all-targets --locked --no-fail-fast` so an earlier intended supplier RED cannot suppress later integration binaries; any failing target still fails the job. The workflow contract requires the exact canonical step and rejects the prior fail-fast stanza. Both composition-root fixtures bound origin acquisition with finite socket I/O, cap request/response evidence at 64 KiB, and require an exact HTTP/1.1 200 status token rather than accepting numeric-prefix lookalikes.

Released Pingora 0.9.0 contains the upstream correctness successor, so the next Runtime Isolation edge is no longer “wait for a maintainer-integrated source repair.” It is `qualify an admissible exact 0.9.0 package/commit identity → ordinary gateway pin/lock bump → unchanged #70 correctness GREEN with admitted in-flight drain preserved`. The GitHub Release object's `immutable:false` state prevents publication alone from satisfying CWL's immutable-release requirement. Independent load/OCI/Supply Chain/bounded-origin capacity lanes remain necessary on every changed exact head and cannot substitute for the correctness assertion.

The many-core contention half of #46 remains separate: realistic configured service/runtime worker counts, parked/reused keep-alive concurrency, shutdown wall time and CPU/off-CPU contention evidence are still required before commercial scaling credit. #969 remains useful historical provenance but is not a dependency authority.

## Capability state and buyer-visible gaps

| Area | Current state | Remaining acceptance |
| --- | --- | --- |
| Admin Config / network authority | Characterized pg-erd transport binding is fail closed; routes remain compiled; final #27 rejects upstream aliases of traffic/metrics listener authority; #39 versions only the explicit response-body lifetime | Revalidate the invariant and v1/v2 boundary on every changed descendant/release candidate |
| Generic forwarding trust | Sanitizer/reconstruction invariant is inherited through the current parent stack | No client-IP/trusted-proxy claim until separately characterized |
| Request-header admission / lifetime | Pinned and released Pingora HTTP/1 has finite supplier parser ceilings and per-read inactivity, but neither is an operator-controlled CWL parser byte/count budget or monotonic whole-header deadline | #43/#993 must supply supported parser-phase byte/count control; #45/#447 must supply supported whole-request-header lifetime in a later released supplier. Keep both distinct from #40/#992 upstream response-header lifetime and from HTTP/2 decoded header-list accounting |
| Runtime isolation / recovery | Body/in-flight rejection, refused origin, read stall, partial response, graceful in-flight drain, pre-header reset, post-commit reset and final #39 body-progress lifetime have exact retained-head technical evidence; #70 carries compiled generic and bounded pg-erd parked-read correctness assertions; released Pingora 0.9.0 contains the upstream shutdown correctness successor | Qualify an admissible exact 0.9.0 identity, ordinary pin/lock bump, and make unchanged #70 assertions GREEN; separately prove configured-worker many-core contention/profile evidence, downstream incomplete-header whole-lifetime, upstream incomplete response-header lifetime, broader admitted long-lived streaming and rollback traffic |
| Upstream TLS | Final #37 has dedicated pg-erd matching-CA/SNI success and hostname-mismatch fail-closed hosted evidence | Representative TLS/network performance and downstream TLS remain unproven |
| Protocols | Final #29 rejects uncharacterized HTTP/1 Upgrade before admission/route/upstream selection; final #33 additionally denies Upgrade at immutable peer construction | Downstream TLS/H2, H2→H1 Cookie behavior, versioned WebSocket/Extended CONNECT and explicit H3/QUIC disposition remain separate contracts |
| OCI / supply chain | Final #44 independently closed exact-head dual-profile OCI and Supply Chain evidence; #70 independently re-executes its changed-head lanes; released Pingora 0.9.0 still declares `derivative 2.2.0` | Every changed descendant must independently revalidate; #889 derivative removal and unchanged #54 absence GREEN remain required; immutable registry digest, signing/attestation/provenance, release-bound SBOM, reproducibility receipt and rollback rehearsal remain gaps |
| Performance | #22 has closed routed Rust-origin aggregate/per-route/sample-floor acceptance; final #44 revalidated the distinct 16-VU/1600-iteration bounded-origin capacity contract inherited from #42 | Controlled loopback is not production SLO proof; #46 still requires configured-worker shutdown contention/profile evidence, and representative TLS/multi-hop/container scheduling/origin-capacity deployment measurements remain required |
| Observability | #23 proves payload-free shared observability; final #31 proves process-wide Pingora-family diagnostic redaction; final #39 requires exact request-error telemetry on lifetime breach | Revalidate on changed descendants; tracing remains separately unproven and product logging stays product-owned |
| Documentation / review | Baseline and focused shutdown traceability distinguish the old admitted-pin RED from published Pingora 0.9.0 release state without claiming runtime closure | Every changed #70 head must reacquire current-head hosted/security/capacity evidence and current-range technical review; owner/bot technical evidence does not replace #56 independent human approval |
| Release / migration | Pingora 0.9.0 is published, but no protected gateway release or consumer cutover credit exists | Exact immutable/provenance-bound supplier qualification where required → applicable consumer REDs GREEN → immutable gateway release → parity → shadow/canary → rollback rehearsal → cutover → verified Nginx/OpenResty/legacy removal |

## Execution order

The current dependency order is `#54 derivative RED + #62 exact semantics/load GREEN → maintainer-integrated derivative removal in a later release-qualified supplier → gateway supplier bump and committed lock regeneration → #54 GREEN + preserved #62 GREEN → #56 independent APPROVED/governance → protected foundation integration → retained pg-erd stack through final #47 on promoted ancestry → released Pingora 0.9.0 shutdown successor qualification → ordinary exact gateway pin/lock bump → unchanged #70 correctness GREEN with admitted in-flight drain preserved → configured-worker contention/profile disposition → #43/#993, #45/#447 and mixed-protocol supplier roots closed on later released source → unchanged consumer REDs GREEN → immutable gateway release/SBOM/provenance/reproducibility/rollback → shadow/canary → cutover → verified legacy removal`.

No Draft state, predecessor receipt, bot review, local image ID, mutable supplier PR, queue state, release name alone or controlled loopback measurement is treated as protected merge, release, canary, cutover, rollback or legacy-removal evidence.
