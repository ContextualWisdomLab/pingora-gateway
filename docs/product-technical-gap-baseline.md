# Product / Technical Gap Baseline

This document is the code-current commercial baseline for `ContextualWisdomLab/pingora-gateway` as of 2026-09-20 KST. It records durable responsibility boundaries, product/technical gaps, acceptance contracts, and dependency order. Mutable PR heads, workflow run IDs, artifact digests, queue state, and branch tips are operational snapshots; issue #58 and the owning PR/Issue are the live exact-evidence ledger and supersede stale identities here.

## Product and DDD responsibility boundary

`pingora-gateway` is a Supporting/Generic edge-runtime subdomain. It owns reusable Ingress, Edge Routing, TLS transport policy, HTTP Policy, Load Balancing, Observability, Admin Config, and Runtime Isolation only where those concerns are genuinely shared edge responsibility.

The Edge Contract bounded context owns network-authority admission. `GatewayConfig` is the aggregate root; listener authority, upstream authority, TLS identity, request-body limits, and I/O budgets are invariants admitted before network activation. Pingora-specific types are delivery details. `GatewayCommand` is the startup application service and Pingora Delivery is the anti-corruption adapter.

The gateway does **not** own product authentication/authorization, tenant or business routing, Keyverse identity authority, Wardnet/EgressWeave policy authority, certificate issuance/private-key custody, application workflow state, or product-specific semantics. Those authorities remain with their canonical owners and are consumed only through released/versioned contracts or explicit ACLs. Source copies, cross-service application SQL, mutable sibling-PR dependencies, and hidden Shared Kernels are rejected.

Legacy Nginx/OpenResty presence is not itself a migration trigger. Static-file serving, FastCGI, certificate issuance/key custody, product authentication, and domain-specific routing stay with the correct owner unless a separately evidenced shared-edge responsibility exists.

## Evidence and promotion invariants

Promotion is based on exact behavior, not release naming, mutable contributor branches, historical GREEN, or documentation claims. The normal sequence is `realistic RED → minimal causal repair at the correct owner → exact-head GREEN → normal/protected integration → release-qualified dependency identity → consumer pin/lock transition → unchanged consumer GREEN → protected integration → release/cutover evidence`.

Predecessor execution and review do not transfer across a new exact head. Draft/Proposed work is not accepted merely because a bot or candidate branch is GREEN. Do not force-push, destructively rebase, self-approve, weaken required checks, suppress advisories, reduce samples/concurrency, artificially warm measured paths, hand-edit generated lockfiles, or pin mutable supplier PRs to manufacture promotion evidence.

Owned production rustdoc, tests, and edge-case coverage remain fail-closed. Applicable buyer-path performance uses compiled Rust and realistic traffic with p95 `<= 20 ms` without sample reduction or measurement exclusion. Controlled loopback evidence is a regression bound, not an Internet/TLS/WAN or representative NUMA production SLO.

## Current authority snapshot

Protected CWL `main` remains `f8b4c99b8e5d3de79af1ff0c00c0c8fd63b52991`, outside the dependency-ordered candidate stack. The repository still has no GitHub Release object, so the product is an implemented candidate, not a released edge product.

Public supplier authority is split deliberately. Protected `cloudflare/pingora/main` is prospective source authority; the latest published dependency release remains Pingora `0.9.0` from 2026-09-09. Its GitHub Release is mutable (`immutable=false`) and has no attached assets, so downstream package authority must bind exact registry source/checksum plus Cargo-generated lock evidence rather than the GitHub Release object alone.

Pingora 0.9.0 closes the previously characterized graceful-shutdown lost-wakeup defect, but does **not** close the current `derivative 2.2.0 / RUSTSEC-2024-0388` intake root, configurable monotonic whole-H1-header lifetime, configurable H1 parser admission, H2→H1 Cookie normalization, zero-length H1 chunk-terminator defect, Brotli response finalization, or the HTTP/1 Upgrade/WebSocket fast-`101` race. Those remain separate supplier-owner paths.

## Foundation and supply-chain security root — #1 / #54 / #62 / upstream #889

Foundation #1 remains Draft at `38db1949354f5721dc0ecfeea395bcf958a64ace`, the normal merge identity of Rust 1.98.1 compiler child #56.

SAST `35426685280` is terminal **SUCCESS**. Security Scan `35426685265` is terminal **FAILURE** for exactly one PR-introduced OSV finding, `Cargo.lock: derivative@2.2.0 / RUSTSEC-2024-0388`; protected base `main@f8b4c99...` produced zero OSV findings. Scorecard, dependency review and Trivy succeeded. This is supplier-admission RED, not scanner/bootstrap or compiler failure.

Local CodeQL required run `35426685246` is terminal **FAILURE in deliberate handoff semantics**, not a vulnerability verdict. Coordinator `105921625299` acquired a runner and dispatched the exact scan. Central `.github@e6334e229581a918e2f22de18733b76fa65d7e71` repository-dispatch `35471064725` is now terminal **CANCELLED before execution**, not queued: `validate-dispatch` `105972000223` ended with `runner_id=0`, `steps=[]`, while downstream scan `105996461669` and settlement `105996461789` also ended without execution. No current-head SARIF verdict exists and no direct cancellation cause is inferred. Queue/admission RCA belongs to `.github#712`; source-neutral wake commits, blind reruns, runner-selector changes, timeout inflation, or local copies of central workflow authority are not accepted repairs.

Upstream #889 remains the supplier owner for `derivative`. Acceptance requires maintainer-integrated removal from the relevant production graph while preserving characterized Pingora semantics, followed by a release-qualified supplier identity. Advisory suppression, deleted lock evidence, CWL forks, or mutable contributor pins are not accepted. #54 remains the package-absence RED owner and #62 the supplier semantics/load/runtime control; their historical valid deltas remain preserved but need ordinary reconciliation and exact GREEN after a release-qualified supplier transition.

## Release compiler root — #56

#56 is complete, not an open prerequisite. Exact `4f2f12de110aa5ee066584c3550606267fdac09e` independently completed CI `35305409034` and Supply Chain `35305409030` under Rust `1.98.1`, including formatting, all-target compile/test, strict Clippy, warnings-denied rustdoc, complete owned-production coverage, load, least-privilege OCI runtime, resolved-lock verification, SBOM/image-scan/source binding, and technical review. It normally merged into foundation as `38db1949354f5721dc0ecfeea395bcf958a64ace`.

The remaining foundation promotion failure is supplier Security plus incomplete current-foundation CodeQL evidence, not compiler governance.

## Shared intermediary / Runtime Isolation stack

#15 normally integrated as `840b03873d8e25ebd960438183884ea609b173ff`, preserving forwarding-header sanitization, RFC 9110 `Via`, TRACE/OPTIONS `Max-Forwards`, bounded intermediary-local outcomes, and pg-erd forwarding compatibility without moving product auth/business authority into the gateway.

#16 normally integrated runtime-isolation acceptance as `bf38bc571dbb7c75e651bcaccdc66904c8c92dab`. Verified successor #119 repaired the refused-origin fixture and normally integrated as `40e10607601bf9722d642d4a4bb18f02806c20cc`. #18 exact `8c0d32332388fc0e398ee3f7b47c6810e72f24d8` completed CI `35448764275` and Supply Chain `35448764235` SUCCESS and normally integrated connected read-stall acceptance as `c78a296a87972845f8f9724e2cf2072d95a7df34`; its controlled loopback load receipt recorded 400 requests, zero failed-request rate, and p95 `1.2343667 ms` against `<20 ms`.

The next dependency-rooted state is now:

- #19 exact `3194f49a4218986ff5d43d0c4c2d275958859efd` completed CI `35467756899` and Supply Chain `35467756872` **SUCCESS**, had no review threads, and normally merged with expected-head protection as `45579666771441db48d3522885b65c60ca87d485`. Its pg-erd OCI least-privilege/Prometheus-listener evidence is integrated into its parent lane. #20 has since been ordinarily/non-force reconciled onto current #19 as exact `3ea8575e82f7613a379d5d5d683f302968e11209`, preserving only its three owned graceful-drain paths and reaching ahead 28 / behind 0. It is Ready, but CI `35486351165` and Supply Chain `35486351247` remain sole current-head evidence queued before checkout with no assigned runner or steps; this is incomplete runner-admission evidence tracked by `.github#712`, not GREEN.
- #21 is now ordinarily/non-force reconciled onto current #20 as exact `157e26050f2014642f03016c1b69a9ea1ef3dc49`, with merge-base exactly `3ea8575e...`, ahead 52 / behind 0, and only the intended four-path post-header-truncation delta. Current-parent review found that the historical successful fixture relied on `GatewayProcess::drop` and `Child::kill()`, which conflicted with the current coverage contract because normal SIGKILL can prevent LLVM profile flush. Commit `e2428190fbb3a248cb69048cf76328d92b8202dc` repairs only that acceptance path: the compiled process now exits through SIGTERM inside `V1_TERMINATION_BUDGET_SECONDS`, with hard kill retained only for failure cleanup. #21 is Ready/mergeable with COMMENT-only review `5259897556`; fresh CI `35496786941` and Supply Chain `35496786929` are materialized but remain queued pre-checkout with no assigned runner or steps, so no current-head GREEN is claimed and #21 must not merge ahead of #20.
- #22 is now ordinarily/non-force reconciled onto current #21 as exact `8d624f9b928bc6e0bbb1957dbf914b4d89bdaa14`, with merge-base exactly `157e2605...`, ahead 64 / behind 0, and exactly the historical nine-path routed-load delta. Only `CHANGELOG.md` and `TEST_STRATEGY.md` overlapped current #21 history and were semantically composed; #21's bounded graceful teardown contract is retained while the child replaces the interpreted load origin with bounded Rust tooling and adds dedicated routed k6 acceptance. The controlled loopback gate uses 4 VUs / 400 iterations, exact status/body, zero failed HTTP requests, independent aggregate/backend/frontend p95 `<20 ms`, and at least 198 samples per route; it is not representative production/TLS/multi-hop/container/Kubernetes SLO evidence. #22 is Ready/mergeable with COMMENT-only current-head review `5260067171`; fresh CI `35499387650` and Supply Chain `35499387658` are materialized but remain queued pre-checkout with no assigned runner or steps, so no current-head GREEN is claimed and #22 must not merge ahead of #20/#21.
- #23 is now ordinarily/non-force reconciled onto current #22 as exact `a6e42c1932eb18f6ea5499aaf862e989de643593`, with merge-base exactly `8d624f9b...`, ahead 28 / behind 0, and exactly the intended three-path payload-free observability delta. Current-parent review found that historical successful stderr capture terminated the compiled migration child with `Child::kill()`, which violated the current coverage lifecycle because normal SIGKILL can prevent LLVM profile flush. Fixture-only ordinary commit `5605f36b4e8cf7dda713e8d13a95b76ea73b84f1` repairs successful Unix teardown to SIGTERM plus successful exit inside `V1_TERMINATION_BUDGET_SECONDS`, retaining hard kill only for failure/panic cleanup. The acceptance remains non-vacuous: the backend must receive exact URI/query, Host, Authorization, Cookie and product-context sentinels while shared gateway stderr contains only the bounded completion vocabulary and none of those sentinels; readiness establishes the exact first completion and the routed request advances the count to two. #23 is Ready/mergeable with COMMENT-only review `5260077511`; fresh CI `35499632481` and Supply Chain `35499632540` are materialized but remain queued pre-checkout with no assigned runner or steps, so no current-head GREEN is claimed and #23 must not merge ahead of #20/#21/#22.
- #24 is now ordinarily/non-force reconciled onto current #23 as exact `a958b9eb4bed594e82ebde8b0f49190287e47ef2`, with merge-base exactly `a6e42c193...`, ahead 75 / behind 0, and exactly five effective paths: `CHANGELOG.md`, `TEST_STRATEGY.md`, `tests/pg_erd_upstream_reset_traffic.rs`, `tests/pg_erd_post_commit_reset_traffic.rs`, and `tests/production_path.rs`. Current-parent review found that both reset fixtures still used `GatewayProcess::drop -> Child::kill()` on successful paths; fixture-only ordinary commit `d2349cdd8c1f02303f18f85b5d35c34f9e939dd9` changes normal-success teardown to SIGTERM plus successful exit inside `V1_TERMINATION_BUDGET_SECONDS`, preserving hard kill only for panic/failure cleanup and leaving TCP-reset/status/framing/timing/error/metric/readiness/recovery oracles unchanged. Initial reconciliation `57ca59d42d37ade5b45e34fab7c5c1106610519c` then exposed a stale whole-file replay risk in `CHANGELOG.md` and `TEST_STRATEGY.md`; ordinary follow-up `a958b9eb...` semantically restores current #23 shared-document contracts before adding the reset delta, and preserves the stronger production-path readiness oracle. #24 is Ready/mergeable with no unresolved review threads. Fresh CI `35502514871` and Supply Chain `35502514847` remain queued pre-checkout, so no current-head GREEN or integration credit is claimed and #24 must not merge ahead of #20/#21/#22/#23.
- #102 remains Draft at `b9447d58ce90e58c76a2af4acd2ef99c4648b9b9`. Source repair restricts privileged process-health handling to body-free completed GET/HEAD probes using `Session::is_body_done()`. One substantive review thread still requires real HTTP/2 `HEADERS(no END_STREAM) → DATA` acceptance on `/livez` and `/readyz`; that listener fixture belongs to #75 and must not be duplicated here. CI `35468136887` / Supply `35468136918` were cancelled by Draft transition; later Draft-sync runs `35475322135` / `35475322102` were skipped. None is GREEN.
- #104 exact `9e5d897e0b7b643dc9e93986c2349edc6106f255` completed CI `35468276852` **SUCCESS** across test/OCI/load and Supply Chain `35468276846` **SUCCESS**, including strict private-production rustdoc enforcement, warnings-denied public rustdoc, 100% owned-production line/region coverage and dependency-lock verification. With no review threads and no executable semantic delta, it normally merged with expected-head protection as `7417e7551a496bffee711375171ea7816cfac81a`. The integration is quality-contract ancestry, not runtime capability credit.
- #106 produced a valid exact-head coverage RED at `5f87459ea78c6e9f5dfe98ec38ffa70550f14626`: Supply `35468678900`, load, OCI, compile/test, Clippy and rustdoc were GREEN, but CI `35468678908` failed the 100% region gate. Coverage artifact `10596611208` (`sha256:cb4616ba20c1d65cda642d1205353ebbb1d6513f51e9a676a9c21db5085ec6e7`) isolated one structurally impossible error continuation in the zero-hop pg-erd response path; ordinary commit `1fb5f514411414bff7a738fb64f3327263024a0d` replaced only that continuation with an invariant assertion, without exclusions or gate weakening. After #104 moved the parent to `7417e7551a496bffee711375171ea7816cfac81a`, five overlapping paths were semantically composed in ordinary commits `cf1977a79fe6df81a1aabbf8e52ae011f0a59951`, `9414c40e481b36ea853046ae92d9bc190a6c3174`, `4b1a5cc82c20fda2a969edb98d2b1a9a5d67f2cf`, `d45f807b0c06fb5af8d15f85910a54734aa2f519`, and `10a229dcd16e18feca8380930d3b25c73175338f`. Final ordinary two-parent reconciliation `18c768b50aebaf5bf9ef39d07c03c0599a5b38f0` uses exact #104 as second parent, is ahead 34 / behind 0 with merge-base exactly `7417e755...`, and retains exactly the intended ten Max-Forwards paths while inheriting #104's parent-only private-production rustdoc deltas. Direct pre-repair-to-current comparison shows no functional deletions. #106 is Ready/mergeable with COMMENT-only current-head review `5259799275`; Draft-sync CI `35494531904` / Supply `35494531888` are skipped, while fresh exact CI `35494556815` / Supply `35494556785` remain queued pre-checkout with no runner/steps. No current-head GREEN is claimed.

Parent or sibling GREEN never transfers across changed exact heads. Queue state alone is not a reason for freshness commits or blind reruns.

## Pg-erd response lifetime and bounded-origin capacity — #39 / #42

#39 normally integrated as `511174a9842c187338b6ff885d76f9d44e183129`. It owns pg-erd Admin Config v2 response-body lifetime: one monotonic budget begins at the first non-informational upstream response header, complements per-read inactivity, and does not fabricate a second downstream status after the final response has already been committed.

#42 completed CI `35413351894`, Supply `35413351919`, and bounded-origin capacity `35413351906`, then normally integrated as `fab59139e74b0eec6988174f4a25fc31d7076633`. Its 16-VU/1600-iteration loopback measurement is controlled regression evidence, not WAN/TLS/NUMA production SLO evidence.

## HTTP/1 header resource-control projection — #44 / #43 / #45

#44 has now been ordinarily/non-force reconciled onto integrated #42. Two-parent commit `bd7483de2b6112d2ae52e84f18a28add2616b026` preserves historical #44 ancestry while adopting the exact #42 tree as resolution. Current exact `c60dc4f56958e8db9be3d365ff18e8ac542a191e` is ahead 47 / behind 0 with merge-base exactly `fab59139...` and exactly ten effective documentation paths. Repository-wide TRACEABILITY and this baseline are deliberately projected through dedicated #61 instead of being replayed by #44. COMMENT-only review `5260463574` verifies that no production Rust, workflow, routing, retry, product-auth/business, Keyverse, Wardnet, or EgressWeave authority moved.

The projection keeps three controls distinct and does not claim any of them closed: #43 / supplier #993 is operator-controlled HTTP/1 parser-phase byte/count admission; fixed supplier ceilings or callback-only 431 are not pre-allocation admission. #45 / supplier #447 is a monotonic whole-request-header lifetime; the supplier's relative per-read inactivity timer is not equivalent when fragments continue arriving. #40 / supplier #992 is the separate upstream incomplete-response-header lifetime, distinct from pg-erd v2 response-body progress lifetime. Mutable supplier branches/forks, retained legacy front proxies, timeout reinterpretation, or HTTP/2 decoded-header-list controls are not accepted substitutes.

#44 was moved to Ready only after reconciliation and current-range review. Its Draft-era exact runs on `c60dc4f...` were skipped; fresh Ready-trigger evidence must settle on the unchanged exact before merge, and historical #44 GREEN does not transfer.

## Released shutdown consumer — #70

#70 exact `df5d2f05fc5fbdd94bbfb487283bf2a6d73a55bf` consumes crates.io `pingora = "=0.9.0"` and `pingora-prometheus = "=0.9.0"` with generated lock authority, but its lineage remains stale relative to foundation `38db194...`.

Historical current-branch execution showed Supply, bounded-origin capacity, OCI and load GREEN while coverage-instrumented traffic failed after an old bare-TCP readiness helper produced `ConnectionReset`. Current foundation owns the causal repair: bounded real `/readyz` HTTP 200 readiness plus listener-reservation handoff. Do not duplicate that source in #70.

Required order remains parent-first: settle foundation/supplier roots, reconcile #44/#47 so current foundation readiness and Rust 1.98.1 are inherited, ordinary/non-force adopt only #70's released-supplier shutdown-consumer delta, then reacquire formatting/test/Clippy/rustdoc/100% coverage/load/OCI/Supply/capacity/current-range review. #73 and later release descendants remain Draft until that parent path is current.

## Supplier/protocol roots

- #71 / upstream #447: real-socket RED for monotonic whole-request-header lifetime, distinct from per-read inactivity. Closure requires maintainer integration in a release-qualified supplier identity and unchanged gateway acceptance GREEN.
- #72 / upstream #993/#1000: executable H1 parser-admission RED for oversized field and field-count traffic. Open contributor code is not dependency authority.
- #53 / upstream #901/#936/#976: H2 downstream → H1 upstream Cookie normalization plus the separate zero-length H1 chunk-terminator defect. Each relevant supplier root needs release-qualified authority or an explicit deployment contract making the path unreachable.
- #109/#110: Traefik file-provider `watch=true` does not by itself justify a generic Pingora hot-reload subsystem. Only protected-source/product-owner evidence may establish live mutation as a product requirement; otherwise use versioned startup configuration plus controlled restart/redeployment.
- #113 / upstream #946/#947: dedicated characterization already reproduces Pingora 0.9.0's fast-`101` WebSocket tunnel teardown as expected supplier RED. The missing element is a maintainer-integrated, release-qualified supplier identity that makes the unchanged oracle GREEN. HTTP/1.1 WebSocket, H2 Extended CONNECT, and H3 Extended CONNECT remain separate transport contracts.
- #115: strict Brotli decoder characterization remains expected released-supplier RED on Pingora 0.9.0. Production compression remains absent/fail-closed until a release-qualified supplier repair makes the unchanged oracle GREEN.

## Buyer-visible release and cutover gap

`pingora-gateway` still has no GitHub Release inventory. A release-ready exact protected head must establish, without predecessor transfer:

- version, CHANGELOG, tag/package, and an immutable gateway release/artifact;
- SBOM, authenticated provenance, reproducibility, source/binary/image identity, and rehearsed rollback;
- non-root/read-only runtime plus supply-chain/security gates;
- TLS, HTTP/1.1, HTTP/2 and applicable HTTP/3 behavior;
- WebSocket/streaming only where an actual migrated product requires it and only after release-qualified supplier/transport acceptance;
- timeout/retry/backpressure, header/cookie/client-IP/body-limit, health/drain, and realistic failure traffic;
- exact-head owned production rustdoc, tests, and edge-case coverage;
- realistic concurrency/load with applicable buyer-path p95 `<= 20 ms`, no artificial warm-up/sample removal, and representative NUMA evidence where the claim depends on it;
- parity → shadow/canary → observed rollback → cutover → verified legacy Nginx/OpenResty removal for each responsibility actually migrated.

Pages publication, contributor CI, loopback p95, mutable supplier metadata, or disappearance of Nginx/OpenResty strings is never sufficient by itself.

## Current causal order

The dependency-rooted order is:

`foundation #1 deterministic Security RED + central CodeQL cancellation/no verdict → .github queue/admission RCA and fresh exact CodeQL verdict → maintainer-integrated/release-qualified #889 repair → #54/#62 ordinary reconciliation and absence/semantics/security revalidation → foundation-equivalent CI/Supply/SAST/Security/CodeQL/current-review GREEN → remaining owner prerequisites and current migration children → normal protected foundation integration → release-line inheritance through #44/#47 → #70 exact revalidation → protocol supplier roots consumed only when maintainer-integrated, release-qualified and required → versioned transport acceptance for actual consumer needs → immutable gateway release/provenance/reproducibility/rollback → representative parity/shadow/canary → cutover → verified legacy removal`.

Independent traffic children move only after their direct parents are current and must reacquire exact evidence after any head change. A failed check is RCA/fix/rerun work, not a reason to weaken the gate. Concurrent movement must be read and adopted/adapted through ordinary non-force ancestry repair rather than treated as a race.
