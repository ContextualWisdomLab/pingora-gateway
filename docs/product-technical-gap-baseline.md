# Product / Technical Gap Baseline

This is the code-current commercial-development baseline for `ContextualWisdomLab/pingora-gateway` as of 2026-09-05 KST. Mutable PR heads are evidence candidates, not release authority; later live evidence supersedes exact identities below.

## Ownership boundary

`pingora-gateway` owns Ingress, Edge Routing, TLS transport policy, HTTP Policy, Load Balancing, Observability, Admin Config, and Runtime Isolation. It may own forwarding sanitation, connection/request limits, timeout/retry/backpressure, health/drain, payload-free low-cardinality transport telemetry, and immutable edge packaging.

It does not own product authentication/authorization, tenancy/business routing, Keyverse identity authority, Wardnet/EgressWeave authority, certificate issuance/key custody, or application-specific FastCGI/business semantics. Cross-context behavior is consumed through released contracts or explicit ACLs; source copies, cross-service application SQL, mutable sibling PR dependencies, and hidden Shared Kernels are rejected.

## Buyer-visible release gaps

The Rust/Pingora gateway is an implemented candidate, not a released edge product. Production host/SNI/routing parity, H2/H3/TLS/WebSocket/streaming failure traffic, immutable packaging, SBOM/provenance/reproducibility, observed rollback, and realistic buyer-path p95 ≤20 ms evidence remain release gates. Protected `main` remains `f8b4c99b8e5d3de79af1ff0c00c0c8fd63b52991`; GitHub Releases is empty at the latest verified sweep.

## Workflow-admission dependency root — #58 / #59 / #60

Merged #57 placed valid workflow-coalescing policy inside #53's H2→H1 Cookie lineage. Both deltas must survive. Workflow policy is promoted at the earliest workflow-owning foundation ancestor, then descendants are repaired by forward adoption and ordinary multi-parent ancestry. Force-push, destructive rebase, simple closure, test deletion, self-approval, and gate weakening are not repair mechanisms.

Draft #59 is based on foundation #1 `5a62e2fa56fdaa6f97c0518932711739e347c04a`, current exact head `cb458621b5cfdcec35820083bb82e19e9dc627cf`, with exactly three Rust workflow contracts in its effective range. Its current Draft synchronize emitted CI `33965929673` and Supply Chain `33965929586`; runner-facing jobs materialized instead of being skipped, so Draft-admission RED remains current before checkout. Fresh exact-range CodeRabbit review of the current head reports no actionable finding; that is technical review evidence, not an approving human review.

Ready #60 predecessor `f1b09eb0a669a6f9f439daf250b1c7d0d95b6c1a` finally acquired hosted `ubuntu-24.04` runners. In CI `33952390050`, `load-contract 101269487174` and `oci-runtime 101269487203` completed success; `test 101269487247` passed formatting and `cargo test --all-targets --locked`, including all 13 workflow-concurrency tests, all 5 semantic Draft-admission tests, and all 5 tag-filter tests. It then failed only at Clippy `question_mark` in the inherited parent oracle, before rustdoc/coverage/lock steps. Supply Chain `33952390006` / `candidate-evidence 101269487216` separately completed success after dependency audit, exact image build, SPDX SBOM generation, image scan, source binding, and evidence upload. This establishes semantic workflow GREEN plus load/OCI/Supply Chain GREEN for that predecessor, not terminal all-gates GREEN.

The Clippy finding was causal and minimal: `tests/workflow_job_admission_contract.rs` used `let Some(on) = document.get("on") else { return None; };`, which is semantically equivalent but rejected by Rust 1.98.0 Clippy under `-D warnings`. Parent #59 commit `cb458621...` replaces only that expression with `let on = document.get("on")?;`. #60 forward-adopted the identical blob at `d49ef52700b0cbb92c4856f2cb3e32fe301532b3` and then recorded current #59 by ordinary two-parent commit `e2309106eba4f41f45c770676885b27bf73aa69b`; ref advancement was non-force. No workflow policy or gateway behavior changed in the lint repair.

Current Ready #60 exact head is therefore `e2309106eba4f41f45c770676885b27bf73aa69b`, based on current #59. Its effective child range remains exactly `.github/workflows/ci.yml` and `.github/workflows/supply-chain.yml` (17 additions / 9 deletions). Current CI `33966008873` and Supply Chain `33966008876` are independent exact-head gates; CI jobs `101306149126`, `101306149265`, and `101306149369` have materialized and remain queued with zero steps/no runner at the latest read. Predecessor terminal results do not transfer. Fresh exact-range CodeRabbit review of the current head reports no static findings and confirms the effective child range remains those two workflow files; terminal hosted all-gates GREEN and then-live governance are still required.

PR #61 is a Draft documentation-only child of #60. After forward-adopting the inherited lint repair and ordinary non-force ancestry repair, its effective range remains exactly `CHANGELOG.md`, `SECURITY.md`, `THREAT_MODEL.md`, `TRD.md`, `docs/doctoring/TRACEABILITY.md`, and this baseline. Draft synchronize runs that complete skipped are descendant admission parity only, not a substitute for #60 Ready-state all-gates GREEN.

Promotion order remains `#59 current lint-clean contract evidence + review → #60 current all-gates GREEN + review → ordinary foundation adoption → #52/#53 non-force ancestry repair`.

## Compiler and supply-chain roots — #56 / #54

Draft #56 is current exact `892f61a05690c10dab110a260e23afa2d94d3c69` on foundation #1 with 12 effective files. Its executable compiler-authority repair is code-identical to GREEN `063ee35f372fa566747f1c489edf5a19b9ee046e`; current head adds only primary POSIX traceability. The review/audit chain has repaired the following parser and authority classes without weakening the fixed Rust 1.98.1 contract:

1. YAML workflow/job/step `env` rejected `RUSTC` and `CARGO_BUILD_RUSTC` but not `RUSTUP_TOOLCHAIN`; `8f03a6ca...` added semantic YAML rejection and a step-level `RUSTUP_TOOLCHAIN: 1.98.0` regression.
2. Shell authority parsing missed Bash compound assignments because `RUSTC+=...`, `CARGO_BUILD_RUSTC+=...`, and `RUSTUP_TOOLCHAIN+=...` produced names ending in `+`; `905ce172...` reduces `=` and `+=` forms to the base variable and adds workflow/Docker regressions.
3. The Docker build boundary used raw `match_indices("cargo build")`, so a comment could impersonate the only build position while a later `cargo<TAB>build` or `car"go" build` escaped the guarded span. `1e2d6ab3...` extracts executable shell-form Docker `RUN` bodies, excludes comments/metadata, normalizes shell words, finds semantic Cargo build authority, and tests both obfuscated executable forms after a forbidden authority change.
4. Whitespace-only shell tokenization could hide compiler authority following unspaced control operators and wrappers. RED `f7803cd73c3f7a9660ce0f51215089837aa8f085` exposed environment selectors; GREEN `07c5b9a7...` added operator-aware command segmentation. RED `6f6f9a43c15cc88f136c75e7e5b2478e2d4bad32` then proved the same boundary could hide explicit `cargo +<toolchain>` and `rustup` selector commands; GREEN `275179a5...` added selector-command checks and normalized Docker `RUN` scoping. Fresh exact review found a valid High where the shell `command` builtin's `-p` option was mistaken for the wrapped executable. RED `dbc4b4912acaba09c36e9c30899641595f884756` covers `command -p env` for all three compiler variables plus Cargo/rustup and `--` wrapper forms. GREEN `013d0586...` skips `-p`, honors `--`, treats `-v`/`-V` as query-only, and then applies the existing compiler-authority checks to the wrapped executable.
5. GNU Coreutils `env -S` / `--split-string=STRING` performs a second argument-splitting pass. RED `f97ce3c9949486a2c7f7e9f802e35acd7614ea48` records direct `env` and `command -p env` specimens. GREEN `128ccc1...` fails closed on explicit split-string forms rather than reimplementing Coreutils parsing semantics.
6. Exact review of `128ccc1...` found that GNU short options can be bundled: `env -iS '<string>'` still invokes split-string. RED `1414a62d5e5613f0bf4eec0cedf66bc7b1b1c33d` adds direct and `command -p env -iS` regressions for `RUSTC`, `CARGO_BUILD_RUSTC`, and `RUSTUP_TOOLCHAIN`; GREEN `5bf6e025...` closes that High.
7. The exact follow-up review found a Medium false positive because the oracle scanned all child-command arguments as though they were still `env` options. RED `069a1423195a23144678d42625e77308db238383` proves that `env -i /usr/bin/printf -S` must remain valid; RED `4aa3aa240a23c0f5228a6e10363f761c9f8a0744` proves that child arguments resembling compiler assignments are not `env` assignment operands. GREEN `137e8e5b...` parses only the documented `env` option/assignment prefix and stops at the first child command.
8. A fresh source audit then found the opposite boundary error at GNU's end-of-options marker: the oracle returned `false` immediately on `env --`, even though `--` only ends option parsing and later `NAME=VALUE` operands still alter the child environment until the first command operand. RED `1576582970509a69bb69fabd9e4bf523a8b0f3a0` adds direct and `command -p env --` regressions for all three compiler authorities while leaving the faulty early return in place. GREEN `cfebacfe2007050f94121dcdcb15fb2c0ead3401` changes only that boundary so `--` disables option parsing but assignment-operand inspection continues until the first child command. The positive control `env -- /usr/bin/printf RUSTUP_TOOLCHAIN=1.98.0` remains allowed because the assignment-looking token is then a child argument. GNU Coreutils 9.11 *Common options* and *env invocation* are the primary authority.
9. A new source audit found that legacy shell backtick command substitution was still executable but invisible to the test-local parser. Committed RED `ba1140fac15e815220407c94fbb1e2aef9776efa` adds ``echo `RUSTUP_TOOLCHAIN=1.98.0 cargo build --release --locked` `` while leaving the parser unchanged. Code GREEN `063ee35f372fa566747f1c489edf5a19b9ee046e` fails closed on an active unescaped backtick outside single quotes rather than incompletely reimplementing nested legacy substitution; a single-quoted backtick literal remains allowed. POSIX.1-2024 Shell Command Language §2.6.3 is now linked in `docs/doctoring/TRACEABILITY.md` because it specifies both `$(commands)` and the backquoted form as executable command substitution. Existing repository workflows do not rely on active backticks, while `$()`/functions/control syntax already used by release scripts is left unchanged.

All predecessor review evidence predates current `892f61a...`. Fresh exact review of `5a62e2fa...892f61a` has been requested and does not inherit older review credit. The committed backtick RED head emitted CI `33968879339` and Supply Chain `33968879304`, but both were cancelled before semantic execution when the GREEN successor arrived; this is source-level RED evidence only. Code GREEN `063ee35f...` emitted CI `33968920941` and Supply Chain `33968920824`, but current traceability head supersedes it for promotion. Current `892f61a...` emitted CI `33969170167` and Supply Chain `33969170158`; hosted current GREEN remains uncredited until actual checkout/execution reaches terminal evidence.

Draft #54 has been non-force restacked onto current #56 at exact `61b26356919989ea8555e58337279e60286e9e78`. `659c6171...` forward-adopted the exact compiler guard and `114a1182...` recorded code GREEN #56 as an ordinary second parent. Parent POSIX traceability was then adapted into the child's existing `derivative` evidence at `2e2c355c...`, and current ordinary two-parent `61b26356...` records #56 `892f61a...`. Fresh compare is ahead 50 / behind 0 with current #56 as exact merge base and exactly four effective child files: `CHANGELOG.md`, `TEST_STRATEGY.md`, `docs/doctoring/TRACEABILITY.md`, and `tests/supply_chain_policy.rs`. No compiler-contract delta remains in the child range. The `derivative` lock assertion is intentional RED only after #56 independently proves compiler/bootstrap GREEN. Fresh exact-range review of `892f61a...61b26356` has been requested; predecessor review does not transfer.

Do not add an audit ignore, suppress OSV/RustSec, delete lock evidence, or pin mutable supplier source to manufacture supply-chain GREEN. `RUSTSEC-2024-0388` remains an INFO/Unmaintained advisory with no patched version; the release block is CWL supply-chain policy rather than a memory-safety-CVE claim.

## Protocol / supplier path

Draft #53 remains exact `bf5436a7b482fffd2c22fb847672076d1063a26a`, open/Draft/mergeable, with five changed files. Its H2→H1 Cookie real-wire fixture remains one file, but four #57 workflow deltas still contaminate the effective range. Foundation workflow repair must reach #52/#53 and restore protocol-test-only scope before supplier RED/GREEN, merge, or release credit.

Protected public `cloudflare/pingora/main` remains `09696b51bc59315353d96686355861604d0bb48c` at the latest supplier sweep. Cookie #901 remains open/unmerged/non-mergeable at `b856ddfc6be15f1727601d2d76cb10d2d72f95f0`; body-framing #936 remains open/unmerged/mergeable at `e40ed4cceb0c0ed8c05cc39eb01a8c73dea5497a`; supplier issue #889 remains open. These are mutable supplier evidence, not immutable dependency authority. Required order is `foundation repair → protocol-only H2→H1 Cookie RED → current-main supplier adaptation/review/integration → immutable supplier identity/release → gateway dependency bump → unchanged wire GREEN`.

## Organization Actions owner-plane

Organization-wide Actions authority remains in `ContextualWisdomLab/.github`. Protected `.github/main` advanced to exact `6f8c51d7389c22ebaf294fe8fe9ef495257883c0` through merged #1929/#1932. That owner-side repair parses the trusted dispatcher actor allowlist consistently across OpenCode review dispatch, CodeQL dispatch, and PR review-fix scheduling while preserving the invariant that actor and sender must be the same listed identity. It deliberately does not change the allowlist variable itself; deciding which identities are authorized remains an owner-plane decision. Dedicated-owner queue-health #1150 remains open at exact head `e6622a428060194b558929ad651d5b4ae3a9840f`; fresh compare against protected main is diverged, 69 ahead / 16 behind, with merge base `6d7fbebec8aec31d88a30a36e71ca5b3925d241d`, while its body still describes older protected/main and head identities. Pingora does not mutate `.github` source/refs/PR state while its dedicated writer is active.

The Pingora specimen distinguishes correct Draft-policy skipped jobs, leaf-workflow defects that admit Draft jobs into the runner queue, Ready jobs that later acquire a hosted runner, and independently reproducible source/test-oracle defects. The #60 predecessor is now direct evidence that a long pre-checkout queue can eventually execute normally; runner delay must therefore remain distinct from semantic failure. These states must not be normalized into a generic runnerless result.

## Legacy migration / release gate

`linux-cluster-ops` remains dedicated-writer territory. Migration stays release-first: safe extraction and structural inventory in the owner repository → explicit certificate/edge/application responsibility split → immutable `pingora-gateway` artifact → parity/shadow/canary → observed rollback → cutover → verified Nginx/OpenResty removal. The gateway must not absorb certificate issuance/key custody or unbounded product-specific FastCGI semantics merely because the legacy proxy co-located them.

Commercial release credit requires exact protected candidate version/CHANGELOG alignment, immutable tag/package/image, SBOM, provenance, reproducibility, rollback artifact/runbook, and all live governance checks. Current merge, immutable release, canary, cutover, and legacy-removal credit remains zero.

Primary standards and research citations belong in `docs/doctoring/TRACEABILITY.md`; this baseline keeps code-current decisions, exact execution dependencies, buyer-visible gaps, and next actions.
